# Backlog

Real, finishable improvements — the daily agent picks ONE and ships it end-to-end.
Rules: check items off when done, add follow-ups you discover, never do two at once,
smallest reviewable change wins.

**Safety — read this first.** MORT OS is a from-scratch kernel whose behavior can
only be verified by **booting it in QEMU**, and the daily cloud agent has **no QEMU
and cannot build or boot the kernel**. So the agent works on **documentation and
non-behavioral text only** — `README.md`, files under `docs/`, and source code
*comments*. It must **not** change kernel logic (the behavior of any `*.mx` file),
`build.py` behavior, assembly (`*.s`), or linker scripts — those require a local
QEMU boot test that a human runs. Every factual claim in a doc must be grounded in
the actual source (cite `file:line`); if the only useful work would touch kernel
behavior, add a backlog item describing it and stop.

## Documentation
- [x] A `docs/programs.md` reference for MORT OS's user-program format and syscall ABI. Done 2026-08-09: no doc previously gathered the four-syscall ABI (print+newline, print inline, uptime, read-line), the mailbox layout, and the `programs/*.mx` build pipeline (`pstart.s`/`prog.ld`, `build_program()` `build.py:274`-`320`) in one place — it was scattered across a one-paragraph summary in `docs/architecture.md`, an address-only table in `docs/memory-map.md`, and comments duplicated across the three `programs/*.mx` sample files. Wrote a grounded reference: what a program is, the build pipeline cited to `build.py`, a full syscall table cited to `on_syscall()` (`kmain.mx:1860`-`1881`), the calling convention shown via real snippets from `programs/hello.mx`/`ask.mx`, and how `_program_sources()` (`build.py:323`-`328`) auto-discovers any new `.mx` file dropped in `programs/`. Linked from README's "Running programs" section and `docs/architecture.md`'s "Programs and syscalls" section. Verified every `file:line` citation against current source and checked all markdown links resolve (script-checked, not just spot-checked).
- [x] `docs/architecture.md` — the boot chain, execution model, and major subsystems, cited to source. Done 2026-07-29: covers both boot paths (`qemu-system-i386 -kernel` vs. the Limine ISO), the `_start` → `kernel_setup` → `mort_kmain` handoff (`boot.s`, `idt.s`), the `kmain()` init order (`kmain.mx:3589-3625`), the three interrupt sources that drive all post-boot behavior (keyboard/timer/syscall), the four-app desktop dispatch (`g_app`, `switch_app`, `kmain.mx:2839-2850`), and the `exec`/syscall mailbox ABI — linking out to the four existing detail docs rather than repeating them. Corrected two inaccuracies in this backlog item's own description while writing it: the kernel never transitions to long mode (stays 32-bit protected mode throughout — verified in `boot.s`/`idt.s`), and the filesystem is MortFS, not "VexFS" (Vex is the unrelated browser-app tribute, `g_app == 2`). Also confirmed, contrary to the item's wording, there is no scheduler anywhere in the source — documented that absence explicitly instead of describing a scheduler that doesn't exist. Linked from README's "How it fits together" section.
- [x] `docs/memory-map.md` — a single table of every fixed address the kernel uses, each row citing the source line that defines it. Done 2026-07-26: kernel load (1 MiB), VGA buffer (`0xB8000`), disk buffers (SECBUF/TABCACHE/FILEBUF), syscall mailbox (`0x009F0000`-`0x009F0179`), program load base (`0x00A00000`, not `0x01000000` — that address is actually the heap base per `kmain.mx:1043`, so this item's own description had the wrong address; corrected in the doc), heap base/top. Framebuffer address is dynamic (read from multiboot info), noted as such rather than listed as fixed.
- [x] `docs/shell.md` — a reference table of the built-in shell commands (read them out of `kmain.mx`), one line of behavior each. Done 2026-07-25: every command in `run_command_impl` (`kmain.mx:2100`-`2602`), cited by line, plus `$VAR` expansion and command history.
- [x] `docs/networking.md` — the mortnet layers (RTL8139 → ARP → IPv4 → UDP → DHCP → DNS → TCP → HTTP) and which `net/*.mx` file implements each. Done 2026-07-28: a layer table citing every builder/parser function by `file:line`, plus a "what `net`/`httpd` actually do" section tracing the real runtime path through `net/netapp.mx`'s `net_dhcp`/`net_httpd`. Linked from README's Networking section. Also noted, grounded in a grep across the repo: `net/dns.mx` is fully implemented and host-testable but is never called from the kernel (no shell command or `netapp.mx` path invokes it), and none of `test.py`/`test_fs.py`/`test_exec.py`/`test_gfx.py` exercise `net`/`httpd` at all.
- [x] README: a "build and boot it yourself" quickstart — the real `build.py` subcommands and QEMU invocation, with the expected first lines of serial output. Verify command names against the actual `build.py`. Done 2026-07-31: the existing quickstart's commands already matched `build.py`'s `COMMANDS` dict, so the gap was the "expected output" half — and there is no serial console in this kernel (confirmed: no serial I/O anywhere in `kmain.mx`/`boot.s`/`idt.s`), so that part of the item's own premise was wrong; documented the real VGA/framebuffer console output instead. Added two grounded transcripts: the bare `-kernel` path (`build.py run`) shows just the boot banner (`kmain.mx:3599`) and prompt, since `run_script(1)` (`kmain.mx:884`) finds no multiboot module without an ISO and no-ops; the `run-iso` path additionally runs the baked-in `STARTUP_TXT` (`build.py:88-90`) via `run_script(1)` at `kmain.mx:3608`, but boots straight to the home launcher overlay (`open_launcher`, `kmain.mx:3362`), so the transcript is only visible after pressing `Esc` — noted that, since a reader who doesn't press Esc would otherwise never see it. Traced every intervening init call (`fs_init`, `fs_ensure_layout`, `fs_populate_bin`, `acct_init`, `login_default`, `cwd_init`) for stray output before writing the transcript; none print anything.

- [x] A `docs/hardware.md` reference for the PCI/USB/audio subsystem (`net/hardware.mx`, `net/hci_usb.mx`, `net/audio.mx`) — the only subsystem with real implementation depth but no dedicated doc, only a one-paragraph README bullet. Done 2026-08-11: wrote a grounded reference covering `hw_scan_pci`'s class/subclass table (`net/hardware.mx:12`-`34`) and that it only runs on-demand from Settings, not at boot; the PC speaker (`net/hardware.mx:36`-`58`); `ethernet_disconnect` (`net/hardware.mx:60`-`66`); the full UHCI boot-time enumeration sequence in `net/hci_usb.mx` (controller find/reset, root-port probing, the hand-built TD/QH control-transfer pipeline for GET_DESCRIPTOR/SET_ADDRESS/GET_CONFIGURATION, and the Bluetooth-HCI-class heuristic that's the only thing behind `g_hw_bluetooth`) — including the "polling until IRQ support is added" comment (`net/hci_usb.mx:205`) confirming it runs before interrupts are enabled; and AC'97 init/volume/test-tone in `net/audio.mx`, also on-demand not at-boot. Caught and fixed one of my own draft claims before committing: I initially wrote that the Settings volume-cycle control could momentarily display above 100, but tracing `(g_audio_volume + 25) % 125` through `ac97_set_volume`'s own clamp (script-verified) showed the clamp happens before the value is stored, so the display never actually exceeds 100 — corrected. Also documented a small but genuine gotcha: Settings' "PS/2 keyboard" row always prints the literal string `"Ready"` (`net/settings.mx:283`) with no actual presence probe behind it. Linked from README's hardware bullet, `docs/architecture.md`'s kernel-entry section, and `docs/networking.md`'s "unrelated to mortnet" disclaimer. Verified every `file:line` citation and markdown link with the same script-based check used in prior passes (0 out of range, 0 broken).
- [x] A `docs/settings.md` reference for the Settings app (`net/settings.mx`, 623 lines) — it had real surface area (13 sidebar sections, 5 interactive sub-pages, a keyword search overlay) but no dedicated doc, only passing mentions in README/`docs/hardware.md`. Done 2026-08-13: read the file in full and wrote a grounded reference: the 13-section sidebar table (which sections are read-only vs. open a sub-page, each cited to its draw function); the `g_settings_view`/`g_settings_section`/`g_settings_sel`/`g_settings_scroll` navigation state machine (`settings_up`/`settings_down`/`settings_back`/`settings_enter`, `net/settings.mx:452`-`545`); the Personalization, Clock, Accessibility, and Ethernet sub-pages (Hardware controls deliberately deferred to `docs/hardware.md`, cross-linked, since that doc already covers it in depth); and the search overlay's fixed 16-keyword index, substring matcher (`settings_matches`, `:78`-`96`), and result-to-section mapping. Caught one genuine drift while grounding the doc: the `g_settings_view` field comment (`net/settings.mx:11`) only listed 3 of its 6 actual values (missing accessibility/hardware/network, added after the comment was written) — fixed the comment in place, kept it on one line so no line numbers after it shifted (verified: file still 623 lines, and existing `net/settings.mx:` citations in `docs/hardware.md`/`README.md`/this file, all below line 11, still match). Linked from README's app-list bullet and `docs/architecture.md`'s window-manager section. Verified every `file:line` citation against current source and checked every markdown link/anchor across `README.md` and `docs/*.md` resolves with a script (0 out of range, 0 broken).
- [x] A `docs/testing.md` reference for the four test scripts (`test.py`, `test_fs.py`, `test_exec.py`, `test_gfx.py`) — README only ever listed the invocation commands, never what each one actually checks. Done 2026-08-12: wrote a grounded reference covering what each script boots/seeds and asserts (read all four scripts in full to write this, not just their docstrings), plus a table documenting the shared harness API `test.py` exports (`boot`/`type_line`/`screen_text`/`wait_for`/`shutdown`) that `test_fs.py`/`test_exec.py` build on. While tracing `test_gfx.py`'s per-app top-bar tab assertions (`test_gfx.py:105`-`140`, checking pixel colors at fixed coordinates for "tab active"/"tab inactive") against current source to cite them accurately, found they test for a feature that doesn't exist in `kmain.mx` as currently written: `draw_topbar()` (`kmain.mx:467`-`473`) draws no per-app tabs (only the OS label, F5/F12 hints, and the clock), and `draw_tab()` (`kmain.mx:453`-`462`) — the helper that would draw one — is confirmed dead code by grep, consistent with this backlog's own existing item about it. Documented the mismatch in `docs/testing.md` rather than editing `test_gfx.py` itself (Python test assertions are outside this agent's docs-only scope, and I have no QEMU to confirm whether the assertion currently passes or fails) — added a follow-up item below for a human to check. Linked from README's testing section. Verified every `file:line` citation and markdown link with the same script-based check used in prior passes (0 out of range, 0 broken).

- [x] `docs/architecture.md` never described how the Mort source actually becomes the multiboot ELF that its own "Boot chain" section boots — `build.py`'s `build()` was undocumented outside a one-paragraph mention in README's ASCII diagram. Done 2026-08-16: added a "Build pipeline" section between "Boot chain" and "Kernel entry" covering all four real steps, each grounded and cited: (1) `net/*.mx` is concatenated with `kmain.mx` into one translation unit before compiling (`build.py:109-122`) — the same glue loop `net/settings.mx`'s header comment already points at; (2) cross-compilation to `x86-freestanding-none` via Zig (`_zig()`, `build.py:96-103`), including a genuinely undocumented "why" worth surfacing: the `-mno-sse`/`-mno-sse2`/`-mno-mmx` flags exist because SSE is disabled at runtime (`CR4.OSFXSR=0`) and an `-O2` vectorized `memset` would otherwise fault (`#UD`) on real hardware (`build.py:127-132`); (3) `runtime.c`'s freestanding `memset`/`memcpy`/`memmove`, compiled and linked in exactly like `kmain.c`/`boot.s`/`idt.s` (`build.py:139-146`, `runtime.c:1-4`) — this file wasn't mentioned in any doc before today; (4) the final link into `build/kernel.elf` and what `build.py check` validates on it (`build.py:148-153`, `check()` `build.py:158-190`). Cross-linked to `docs/programs.md`'s existing build-pipeline section for the parallel (but differently-linked) `programs/*.mx` path. Verified every `file:line` citation against current source; no new links added (referenced files already had entries elsewhere in the doc).

- [x] `docs/architecture.md`'s "desktop / window manager" section named the Files and Vex apps in one clause each with no further detail — everything else about them (navigation, keybindings, graphics-only reachability) was undocumented. Done 2026-08-17: read both apps in full (`files_draw`/`files_draw_content`/`files_open`/`files_back`/`files_on_key`, `kmain.mx:2681`-`2774`; `vex_page_home`/`vex_page_about`/`vex_draw`/`vex_on_key`, `kmain.mx:2778`-`2821`) and added a new "The Files and Vex apps" section between "The desktop / window manager" and "Programs and syscalls", grounded and cited: Files' two-view state machine (list vs. content, `g_files_sel`/`g_files_view`/`g_files_entry`/`g_files_count`, `kmain.mx:50`-`53`) and the no-disk error path (`kmain.mx:2694`-`2698`); Vex's two static pages (`g_vex_page`, `kmain.mx:54`) and that its drawn address bar accepts no input despite looking like one. Also traced and documented a genuine, previously-unstated asymmetry: `switch_app` (`kmain.mx:2839`-`2850`) resets Files' view/selection state on every switch into it, but has no equivalent reset for Vex. Confirmed both apps are entirely unreachable in text mode by re-checking `on_key`'s `g_gfx` gate around the F1-F4 dispatch (`kmain.mx:3503`-`3508`) — `g_app` can never leave `0` without it. Verified every `file:line` citation against current source with the same script-based range check used in prior passes (0 out of range); no new markdown links added (checked all existing links across `README.md`/`docs/*.md` still resolve).

## Doc quality
- [x] Audit `README.md` and `docs/` for claims that have drifted from the current source (feature list vs. what is actually implemented) and correct them, citing the source for each fix. Partial progress 2026-08-06: spot-checked every `file:line` citation in `README.md`, `docs/architecture.md`, `docs/shell.md`, and most of `docs/memory-map.md` against the current source (`kmain.mx`, `boot.s`, `idt.s`, `linker.ld`) — all matched, nothing stale found there. The real drift found was a missing feature, not a stale citation: `net/hardware.mx` (PCI bus scan), `net/hci_usb.mx` (UHCI USB driver), and `net/audio.mx` (AC'97 audio) are fully implemented and wired into the Settings app's "Hardware & Devices" page (`settings_hardware`, `net/settings.mx:280`) but were entirely absent from README's "What it does" list — fixed with a new grounded bullet. Also fixed `kmain.mx:49`'s `g_app` comment, which listed only 3 of the 4 apps (`0 terminal, 1 files, 2 vex` — missing `3 settings`), found while cross-checking the app-switch dispatch (`kmain.mx:3510-3518`) against it. Still unaudited: `docs/networking.md`'s `net/*.mx` citations beyond the NIC driver row, `docs/fs-design.md`'s historical sections 1-5, and `mkfs.py`/`build.py` internals. Further progress 2026-08-07: audited every `file:line` citation in `docs/networking.md`'s layer table and its "What `net`/`httpd` actually do" section (`net/endian.mx`, `net/checksum.mx`, `net/rtl8139.mx`, `net/eth.mx`, `net/arp.mx`, `net/ip.mx`, `net/icmp.mx`, `net/udp.mx`, `net/dhcp.mx`, `net/dns.mx`, `net/tcp.mx`, `net/http.mx`, `net/netcfg.mx`, `net/netapp.mx`, plus `kmain.mx:2131-2137`'s `net`/`httpd` dispatch). All matched except the NIC driver row: `rtl_init`, `rtl_poll_rx`, and `rtl_transmit` were each cited one line past their actual `fn` line (`net/rtl8139.mx:64`/`129`/`175` vs. the real `63`/`128`/`174`) — fixed all three. Still unaudited: `docs/fs-design.md`'s historical sections 1-5, and `mkfs.py`/`build.py` internals. Further progress 2026-08-08: audited `docs/fs-design.md` against `mkfs.py` and the current `kmain.mx` FS code — found the biggest drift yet. MortFS is now v2, not v1: `mkfs.py` writes `version = 2` (`mkfs.py:31`) and `fs_init` (`kmain.mx:1296`-`1332`) auto-upgrades any v1 disk in place on first boot. v2 adds directories, Unix-style permissions (`mode`), and ownership (`uid`) in the 12 bytes of each file-table entry that Section 2.3 called "reserved" — `type`/`parent`/`uid`/`mode`/`mtime`, accessors at `kmain.mx:1269`-`1273`. None of this was in `docs/fs-design.md` at all, and its Section 7 non-goals still claimed "No directories" as current. `docs/shell.md` (audited 2026-08-06, still accurate) already documents the resulting `cd`/`mkdir`/`rmdir`/`chmod`/`chown` commands, so only `fs-design.md` was stale. Fixed: added a grounded "MortFS has since grown a v2 format" note up top citing every relevant function, corrected the Section 2.3 reserved-bytes row, and corrected the Section 7 non-goals bullet (left the stale text visible with a strikethrough-style note rather than silently deleting a direct quote of the original v1 list, per this doc's own established convention for historical sections). Also fixed `mkfs.py`'s docstring and `--help` text, which still said `python kernel/mkfs.py`/`python kernel/build.py`/`kernel/docs/fs-design.md` (comment/docstring only — no argparse behavior changed, verified with `--help`). Still unaudited: `docs/fs-design.md` Sections 1 and 3-6 line-by-line (only the format-version and directory claims were checked against source; the ATA driver section (1) and buffer/shell-spec sections (3-6) were not re-verified this pass), and `build.py` internals. Further progress 2026-08-09: audited `docs/fs-design.md` Section 1 (ATA PIO driver spec) and Section 3 (buffer addresses) line-by-line against `kmain.mx`'s `ata_init`/`ata_wait_bsy`/`ata_wait_drq`/`ata_read`/`ata_write` and the `SECBUF`/`TABCACHE`/`FILEBUF` constants — every port number, status-bit mask, and address matched exactly, including the `0xFC` PIC mask claim (verified against `idt.s:131`). Also mechanically checked every `file:line` citation across `README.md` and all of `docs/*.md` with a script that resolves each citation against the cited file's actual line count (0 out of range) and a second script that resolves every markdown link (0 broken) — a cheap check worth re-running on future passes since it catches the citation-drifted-after-an-edit-elsewhere failure mode that spot-checking misses. Spot-verified `build.py`'s `net/*.mx` glue-loop citation in `net/settings.mx` (`build.py:113`-`121`, still accurate) and the `STARTUP_TXT`/`ensure_disk`/`disk()` citations behind README's ISO quickstart (still accurate). Sections 4-6 remain deliberately unaudited per-claim: the doc's own header already discloses they're historical/pre-implementation prose superseded by `docs/shell.md`, so auditing them further would just restate that disclosure. `build.py` internals are now covered by this pass and the new `docs/programs.md` item above (its build-pipeline section audited `build_program()`/`_program_sources()`/`prog()` in full). Net effect: no remaining unaudited surface found to be materially stale — treating this item as complete unless a future pass turns up new drift after further source changes.
- [x] `docs/fs-design.md` opens with "Nothing here is implemented yet; this is the spec to implement from" (line 3), but MortFS is fully implemented and shipping (`kmain.mx` has `fs_init`/`fs_read_file`/etc., `docs/shell.md` documents `ls`/`cat`/`write`/`rm`/`run` as working commands). Done 2026-07-27: replaced the header with a "Status: implemented" note citing the real functions (`fs_init` `kmain.mx:1296`, `fs_create` `kmain.mx:1587`, `fs_read_file` `kmain.mx:1662`, `fs_remove` `kmain.mx:1732`, `ata_init`/`ata_read`/`ata_write` `kmain.mx:1175`-`1241`, `g_run_depth` guard `kmain.mx:40`/`895`) and flagging that Sections 0-5 are historical (drafting-time paths like `kernel/kmain.mx`/`kernel/mkfs.py`/`mort/typechecker.py` were not rewritten, only the header), Section 6 is the executed plan not a to-do, Section 7's non-goals are current limitations. Body sections 1-5 left untouched per the item's own scope.
- [x] `docs/fs-design.md` sections 1 and 5.1 still say `kernel/kmain.mx` and `kernel/mkfs.py` — the real files are `kmain.mx` and `mkfs.py` at the repo root. Done 2026-08-02: fixed the copy-pasteable commands in Section 5 (`### 5.1 mkfs.py`, `### 5.2 build.py` headers and their code blocks) to the real flat-layout paths, since those are invocations a reader could actually try to run. Left the historical narrative prose alone (e.g. "drafted as `kernel/kmain.mx` below" in the intro) per the doc's own header explaining Sections 0-5 are a kept-as-is historical record — only the runnable bits were broken, so only those were fixed. While grounding the Section 2 quote of the kernel's `bad filesystem` message against the actual source, found it doesn't match verbatim (doc said "run:", source says "host:" — `kmain.mx:1751`); corrected the quote and discovered the source string itself still hard-codes the stale `kernel/mkfs.py` path, which is a genuine source bug (see new item below), not a doc bug — left the quote showing that stale path since it's what the kernel actually prints.
- [ ] `kmain.mx:1751`'s `bad filesystem` message hard-codes a stale path: `"bad filesystem (host: python kernel/mkfs.py disk.img)"` — the real invocation is `python mkfs.py build/disk.img` (`mkfs.py` lives at the repo root, not under `kernel/`, and `build.py`'s own hints at `build.py:345`/`372` point at `build/disk.img`). A one-line string fix for a human with a local QEMU boot test to confirm the new message still fits the print budget; found while grounding `docs/fs-design.md`'s quote of this message against the actual source.
- [x] `power`, `lock`, and `sleep` shell commands silently no-op in VGA text mode — `open_power_menu` (`kmain.mx:3145`), `open_lock` (`kmain.mx:3199`), and `open_sleep` (`kmain.mx:3229`) all `return` immediately `if !g_gfx`. Worth a doc note (or, as a code change for a human with a local QEMU boot test, a `no display` message instead of silent no-op). Done 2026-08-04: this item's own premise was only a third right — read all three functions and only `open_power_menu` actually silently no-ops; `open_lock` and `open_sleep` both print an explicit message (`"lock needs the graphical desktop"` / `"sleep needs the graphical desktop"`, `kmain.mx:3200`-`3203` and `kmain.mx:3230`-`3233`) before returning. Documented the real per-command behavior in `docs/shell.md`'s `power`/`lock`/`sleep` rows, which previously said nothing about text-mode at all. Left the code as-is (no message for `power` in text mode) — a human with a local QEMU boot test could add one, matching the pattern `open_lock`/`open_sleep` already use, per this item's own suggestion.
- [x] `net/rtl8139.mx`'s header comment (line 1) says the file lives at `glue/rtl8139.mx`; the real path is `net/rtl8139.mx`. Same comment also says "M1 does transmit only — receive arrives with ARP in M2" (line 10-11), but receive is already implemented in this same file (`rtl_poll_rx`, `net/rtl8139.mx:129`) — a stale milestone note from an earlier development stage. Small comment fix, found while writing `docs/networking.md`. Done 2026-08-01: fixed the path (`net/rtl8139.mx`), dropped the now-false "lives in `glue/` rather than `net/`" rationale, and replaced the transmit-only milestone note with a line naming both `rtl_transmit` and `rtl_poll_rx` (the latter polled from `net/netapp.mx:79`/`195`, confirmed by grep) as implemented.
- [ ] `net/dns.mx` is a fully implemented, host-tested DNS A-record resolver that no shell command or kernel code path ever calls (confirmed by grepping for `dns_` outside that file) — found while writing `docs/networking.md`. Either wire it up (e.g. an `httpd` variant that resolves a configured hostname, or a `nslookup`-style shell command) as a code change for a human with a local QEMU boot test, or leave as-is and just keep the doc note that it's currently dead code.
- [x] README's "What it does" list only names three apps (Terminal, Files, Vex browser) switched with F1/F2/F3, but the source has four: `on_key` (`kmain.mx:3503-3508`) maps F1-F4 to `switch_app(0..3)` where app `3` is a Settings app (`settings_on_key`/`settings_draw`, dispatched at `kmain.mx:2849`), and F5 opens a home launcher (`open_launcher`, `kmain.mx:3362`) not mentioned in the README at all. Found while writing `docs/architecture.md`; feeds the "audit README for drifted claims" item above. Done 2026-07-30: fixed both the intro paragraph and the "What it does" bullet to name all four apps (Terminal, Files, Vex, Settings — the latter backed by `net/settings.mx`, confirmed a real personalization/clock/network/hardware control center, not a stub) plus the F5 launcher, citing `kmain.mx:3503-3508` and `kmain.mx:3362`.
- [x] `net/settings.mx` implements the Settings app (`settings_draw`/`settings_on_key`) but lives under `net/` alongside the actual networking stack — it's pulled in only because `build.py`'s `build()` (`build.py:110-119`) glues every `net/*.mx` file into the same translation unit as `kmain.mx`. Not a bug (build works fine), just a confusing location for a file with nothing to do with networking; worth a one-line comment at the top of `net/settings.mx` noting why it's there, or a human could move it to a top-level file as a build.py-touching change. Found while verifying the app-list fix above. Done 2026-08-03: added a comment block at the top of `net/settings.mx` explaining the file's location, citing the actual glue loop (`build.py:113-121`, confirmed by reading the current file — line numbers had drifted slightly from this item's original `build.py:110-119`). Comment-only change, no behavior touched.
- [x] `draw_tab()` (`kmain.mx:453-462`) — a helper for drawing a highlighted app-switcher tab in the top bar — is defined but never called anywhere in the source (confirmed by grep); `draw_topbar()` (`kmain.mx:467-473`) only draws static "F5 home"/"F12 power" hint text, no per-app tabs. Either dead code a human can remove, or an unfinished feature; found while verifying the app-list fix above. Done 2026-08-05: `draw_topbar()`'s own header comment was itself stale — it claimed to draw "app switcher tabs (F1/F2/F3)" when the function body never does. Fixed both comments (source-comment-only change, verified line count unchanged so no other doc's `kmain.mx:` citations drifted): `draw_tab()` now notes it's unused, `draw_topbar()` now describes what it actually draws and points at `draw_tab()` as the unused helper. No other doc referenced "app switcher tabs" so nothing else needed correcting. The underlying dead-code-or-unfinished-feature question is still a human call — left as a new backlog item below since it requires a code decision, not a doc one.
- [ ] `draw_tab()` (`kmain.mx:453-462`) is a fully-working unused helper — a code-level decision, not a doc one: either wire it into `draw_topbar()` to actually render F1-F4 tabs (an unfinished feature), or delete it as dead code. Either way needs a local QEMU boot test to confirm the top bar still renders correctly. Left open when correcting `draw_topbar()`'s stale comment (2026-08-05); this item is the follow-up code work the comment fix doesn't cover.
- [x] There is no serial console anywhere in this kernel (`boot.s`, `idt.s`, `kmain.mx` — grepped, nothing touches COM1/0x3F8 or any UART). All console output is VGA text (`put_index`) or a drawn framebuffer console (`draw_glyph`), both behind `put_cell_at` (`kmain.mx:505-517`). Worth a one-line note wherever a reader might assume a serial log exists (there is currently no such assumption in the docs, but the original wording of the README-quickstart backlog item above assumed one — future doc work should not repeat that assumption). Found while grounding the 2026-07-31 README quickstart addition. Done 2026-08-10: added the note right after README's quickstart transcript (the one place a reader watching QEMU output might reach for a serial log instead), citing the same grep result and `put_cell_at` (`kmain.mx:505-517`, verified exact) as the funnel point for both VGA and framebuffer output. Re-confirmed the grep: no `serial`/`0x3F8`/`COM1`/`UART` hit anywhere in `boot.s`, `idt.s`, or `kmain.mx`.
- [ ] The `run-iso` boot path opens straight to the home launcher overlay (`open_launcher`, `kmain.mx:3619`, called unconditionally when `g_gfx`) rather than the terminal, so first-time users booting the ISO see icon tiles, not a shell prompt, until they press `Esc` or `Enter` on Terminal. Not necessarily a bug, but worth confirming is the intended first-boot experience — a human with a local QEMU boot test could decide whether to change it (code change, out of scope for the docs agent) or just leave it as designed. Found while writing the ISO transcript in the README quickstart addition (2026-07-31).
- [ ] `test_gfx.py`'s per-app top-bar tab assertions (`test_gfx.py:105`-`140`) check for pixel colors that no current source path produces — see `docs/testing.md`'s `test_gfx.py` section for the full trace (`draw_topbar()` draws no tabs; `draw_tab()`, the helper that would, is unused dead code per the existing `draw_tab()` backlog item above). A human with a local QEMU + ISO toolchain should actually run `test_gfx.py` to confirm whether these specific checks currently pass or fail. If they fail, this is the same underlying code decision as the `draw_tab()` item (wire it up or remove it) plus a test fix; if they somehow pass, this doc's reasoning has a gap worth finding. Found 2026-08-12 while writing `docs/testing.md`.
- [x] `python build.py iso`/`run-iso` (and `test_gfx.py`, which calls `build.iso()` directly) only work on Windows — undocumented anywhere. Done 2026-08-14: read `build.py` in full looking for fresh doc gaps (BACKLOG's own doc items were all already checked off) and found `iso()` (`build.py:219`-`271`) fetches `limine.exe`/`xorriso.exe` — Windows PE binaries, `LIMINE_ZIP`/`XORRISO_ZIP` at `build.py:65`-`66` — and executes them directly via `subprocess.run` (`build.py:261`-`269`), with `_cygpath()` (`build.py:212`-`216`) converting paths to the Cygwin form `xorriso.exe` expects; no Wine invocation or OS branch anywhere in the script. Added a grounded note to README's "A real bootable ISO" section and to `docs/testing.md`'s harness-requirements paragraph (linking back to the README note), so a Linux/macOS reader hits this before an opaque exec-format failure instead of after. `build`/`check`/`run`/`window`/`disk`/`prog`/`test.py`/`test_fs.py`/`test_exec.py` are unaffected — confirmed none of them call `iso()` or touch `TOOLS`/`LIMINE_ZIP`/`XORRISO_ZIP`.
- [ ] `build.py iso()` fails on non-Windows hosts with a raw `subprocess`/OS-level exec error (attempting to run a fetched `.exe` directly, `build.py:261`/`269`) rather than a clear message. A human with the ability to test on Linux/macOS could add an early `sys.platform` check in `iso()` that exits with a clear "Windows only (or install/invoke via Wine)" message instead — a `build.py`-behavior change, out of scope for the docs-only agent, and small enough to verify without a QEMU boot (just needs running `python build.py iso` on a non-Windows host to confirm the current failure mode first). Found 2026-08-14 while grounding the Windows-only ISO note above.
- [x] `font_data.mxinc` (repo root, 258 lines) declares a `g_font: [u8; 4096]` bitmap font but is never referenced by `build.py` or any `*.mx`/`*.s` file. Done 2026-08-15: all prior doc-item leads in this backlog were either already checked off or explicitly out-of-scope code changes, so this pass re-read the repo root for undocumented surface and found this file by grepping for `font_data` across the whole tree (zero hits outside the file itself). Diffed it byte-for-byte against the `g_font` array actually compiled into the kernel (`kmain.mx:74`-`331`, used by `draw_glyph`, `kmain.mx:420`) — identical. Added a header comment to `font_data.mxinc` itself (comment-only, no behavior change) explaining it's an orphaned duplicate, and a new "Orphaned files" section in `docs/architecture.md` with the same finding, cited and cross-linked. Left the file in place rather than deleting it — deleting an unreferenced file is safe (nothing includes it) but is a repo-structure change outside this pass's README/docs/comments scope; noted below as a follow-up a human can do in one command.
- [ ] `git rm font_data.mxinc` — confirmed dead/orphaned (see item above, `docs/architecture.md`'s "Orphaned files" section, and the header comment now on the file itself). Deleting it changes no kernel behavior since nothing includes it, so this is safe for a human to do without a QEMU boot test; left undone by the docs-only agent since removing a tracked file is a repo-structure change, not documentation.
- [x] `docs/shell.md`'s `export NAME=value` row never mentioned that custom shell variables are capped at 8 slots, and that exceeding the cap fails **silently** (no error, no message — the assignment is just dropped). Done 2026-08-18: every other unchecked backlog item was an explicitly out-of-scope code change (kernel logic, `build.py` behavior, or a QEMU-verified test run), so this pass searched the source directly for undocumented gotchas and found one: `g_env_name`/`g_env_val` are fixed at "8 slots x 32 bytes" / "8 slots x 64 bytes" (`kmain.mx:36`-`37`), and `env_set` (`kmain.mx:1938`-`1955`) silently `return`s (`kmain.mx:1941`-`1943`) when asked to add a *new* variable name past `g_env_count >= 8` — with no feedback printed anywhere in the `export` dispatch (`kmain.mx:2362`-`2371`). Also confirmed re-`export`-ing an *already-set* name still works past the cap, since updates skip the count check (`env_find` finds the existing slot, so the `i == 64` branch containing the cap check never runs). Extended the `export` row in `docs/shell.md` with this, cited to source. Comment/doc-only change; verified the citation ranges against current source.

## Doc quality (found 2026-08-18, not yet done)
- [x] `mkfs.py:60`'s `make()` function docstring still says `"""Create (or overwrite) a MortFS v1 image at \`path\`."""` even though `mkfs.py` writes `VERSION = 2` (`mkfs.py:33`), self-checks `version == VERSION` (`mkfs.py:152` after this pass's edit; was `:148`), and its own success message prints "MortFS v2" (`mkfs.py:134` after this pass's edit; was `:130`). The *module-level* docstring (`mkfs.py:19`-`21`) was already corrected to v2 during the 2026-08-08 pass (see the "Audit README and docs/ for claims that have drifted" item above), but that pass missed this second, function-level docstring 40 lines later. One-word fix (`v1` → `v2`), comment-only, no behavior change — small enough for tomorrow's docs pass to pick up directly. Done 2026-08-20: corrected the `make()` docstring to "MortFS v2" and, since a fresh image is the one case that never hits the kernel's v1->v2 upgrade path, added a sentence noting `make()` writes every entry's v2 metadata directly (file/root-parent/uid 0/mode `0o644`, `mkfs.py:120`-`123`, `struct.pack("<BBBBII", 0, 255, 0, 0, 0o644, 0)`) so the docstring's own claim is verifiable at the code right below it. Also fixed a stale citation this item introduced: the doc claim in `docs/fs-design.md` pointed at `mkfs.py:31` for the version constant, but `VERSION = 2` is at `mkfs.py:33` (line 31 is `SECTOR = 512`) — corrected the citation and named the constant (`VERSION`) so it survives future line drift. Docstring/doc-only change, no `argparse` or write behavior touched. Re-ran the citation/link check across `README.md` + all `docs/*.md`: 329 citations, 0 out of range; 43 links, 0 broken.
- [x] README's "Build & run" section documents `check`/`run`/`iso`/`run-iso`/`prog` each with an explanation, but `python build.py window` (`build.py:432` → `run_windowed()` → `_run(fullscreen=False)`, `build.py:428`-`429`) is only ever name-dropped once, in the aside listing which commands aren't Windows-only (`README.md:155`). Worth a one-line addition near `README.md:71`-`73` alongside `check`/`run` explaining it boots the same kernel+disk as `run` but in a resizable window instead of fullscreen. Found 2026-08-18 while looking for fresh doc gaps after the rest of the backlog was resolved or out-of-scope. Done 2026-08-19: verified `run_windowed()` (`build.py:428`-`429`) calls the same `_run()` (`build.py:403`-`421`) as `run()`, differing only in the `fullscreen` flag (skips the `-full-screen` QEMU arg, `build.py:413`-`414`) — same `build()`+`ensure_disk()`+disk-attached QEMU invocation either way. Added a one-line `python build.py window` entry to the `check`/`run` code block (`README.md:71`-`73`).
- [x] `docs/*.png` had three screenshots (`app-vex.png`, `mortos-home.png`, `mortos-lock.png`) that no `README.md`/`docs/*.md` file referenced (checked with a grep of every `.png` basename across all markdown) — found 2026-08-21 while looking for fresh doc gaps after every unchecked backlog item turned out to be an explicitly out-of-scope code/asset change. Done 2026-08-21: viewed all three. `mortos-home.png` and `mortos-lock.png` are accurate, current screenshots — text-matched byte-for-byte against `draw_launcher()`'s tile labels/hint string (`kmain.mx:3327`-`3359`, "Terminal"/"Files"/"Vex"/"Settings"/"Power", "arrows move   Enter open   Esc close   (F5 = home)") and `draw_lock()`'s copy (`kmain.mx:3168`-`3195`, "Locked"/"type your password, then Enter") — so added both to README: the lock screen next to the existing power-menu screenshot in "Session & power" (both describe `open_lock`/the power menu in the same paragraph), and the home launcher in "What it does" right before the existing Files screenshot (the launcher is the F5 entry point the same bullet already describes). `app-vex.png` turned out to be stale, not just unused: it shows per-app tabs (`F1 terminal`/`F2 files`/`F3 vex`) drawn in the top bar, but `draw_topbar()` (`kmain.mx:467`-`473`) draws no such tabs — confirmed by this backlog's own earlier finding that `draw_tab()`, the helper that would draw them, is unused dead code (`kmain.mx:453`-`462`). Left it out and added a follow-up below rather than publish a doc image that contradicts the already-documented current behavior. Verified every `.png` reference across `README.md`/`docs/*.md` resolves to an existing file (script-checked).
- [ ] `docs/app-vex.png` is a stale screenshot: it shows `F1 terminal`/`F2 files`/`F3 vex` tabs drawn in the top bar, but current `draw_topbar()` (`kmain.mx:467`-`473`) draws no per-app tabs at all (`draw_tab()`, the would-be tab-drawing helper, is confirmed unused dead code — see the existing `draw_tab()` backlog item). Either the screenshot predates a since-removed feature, or it was staged/mocked rather than captured from a real boot. A human with a local QEMU+ISO boot (`python build.py run-iso`, screen-dumped) could recapture an accurate one and add it next to the existing `docs/app-files.png` in README's "What it does" section — the Vex app currently has prose but no screenshot, same gap this pass filled for the home launcher and lock screen. Found 2026-08-21 while adding the other two previously-unused screenshots to README.
- [x] `net/netcfg.mx`'s header comments (lines 10-11, 19 before this fix) said our IPv4 address was "fixed to QEMU's default SLIRP guest address so the demo needs no DHCP (that arrives in M3)" and that `net_set_ip` was "hardcoded at boot, or learned from DHCP in M3" — stale milestone language from before DHCP existed, same pattern as the already-fixed `net/rtl8139.mx` header comment. DHCP is fully implemented (`net/dhcp.mx`) and already wired up: `net_dhcp()` (`net/netapp.mx:46`-`121`) zeroes `g_our_ip` at the start of every lease attempt (`net/netapp.mx:57`-`58`) and calls `net_set_ip()` again with the DHCP-leased address on ACK (`net/netapp.mx:95`) — confirmed by reading `net_dhcp` in full and cross-checking `docs/networking.md`'s own already-correct note about this (`docs/networking.md:61`-`65`, added in an earlier pass) against the two now-corrected comments. Done 2026-08-23: rewrote both comments to describe DHCP as implemented rather than a future milestone, citing the real functions/lines above; comment-only change, no behavior touched. Since the new comment adds lines, updated the two `net/netcfg.mx:` citations that shifted in `docs/networking.md` (`g_our_ip`'s declaration: `:13` -> `:18`; `net_handle_frame`: `:29` -> `:35`) and re-ran the repo's citation/link checks (all `file:line` citations across `README.md`+`docs/*.md` in range, all markdown links resolve).
- [x] The users/authentication/permissions subsystem (`kmain.mx:2852`-`3009`, plus the `can_write` call sites in the FS code) had real depth — an account table, hashed passwords, auto-login, `su`/`sudo`/`passwd`, and owner/mode bits on every MortFS v2 entry — but no dedicated doc: `docs/shell.md` documented the commands one row each, and the only prose about the *model* was a single clause in `docs/architecture.md`'s kernel-entry section. Done 2026-08-24: every remaining unchecked backlog item was an explicitly out-of-scope code change, so this pass read the subsystem in full and wrote `docs/accounts.md`: the in-memory 8-slot account table and its two default accounts (`root`/`toor` uid 0, `mortuex`/`mort` uid 1, `acct_init` `kmain.mx:2900`-`2904`) with djb2 hashes (`hash_pw` `kmain.mx:2860`-`2868`); the three consequences of the table being in-memory and fixed (nothing persists across a reboot, `acct_add` `kmain.mx:2870`-`2885` has no caller but `acct_init` so `su` can only target those two names, and `chown` `kmain.mx:2533` accepts a uid with no account behind it); the boot-time auto-login sequence (`kmain.mx:3603`-`3606`) and why `ensure_home`/`fs_ensure_layout`/`fs_populate_bin` produce root-owned entries (they run before `login_default()` sets `g_uid = 1`, while `g_uid` is still its initial 0, `kmain.mx:25`); the six places the current user surfaces (prompt, `whoami`, `$USER`/`$HOME`, `ls`'s uid column, the launcher's welcome line, and the lock screen); and the permission model itself — `can_write` (`kmain.mx:2974`-`2982`) and its exactly three call sites (`fs_create_full` `kmain.mx:1540`-`1544`, `write` `kmain.mx:2296`-`2300`, `rm` `kmain.mx:2334`-`2338`) plus `chmod`'s owner-or-root and `chown`'s root-only checks. Two grounded findings worth the doc on their own: `lock_ok` (`kmain.mx:3212`-`3219`) checks the *current user's* hash, so README's "the lock password is `mort`" is only true before an `su root` (then it's `toor`) — documented rather than changing README's line, which is correct for the default session; and the mode bits are only partly live — read bits are never consulted (`cat`, `kmain.mx:2258`-`2277`), execute bits are never consulted (`exec_file`, `kmain.mx:1892`-`1920`, runs a `0600` file just as happily as the `0755` one `fs_populate_bin` marks at `kmain.mx:1649`), and `rmdir` (`kmain.mx:2235`-`2257`) makes no `can_write` call at all. Linked from README's "What it does" list and `docs/architecture.md`'s kernel-entry paragraph — and while linking, corrected that paragraph's "the one built-in user account", which has been wrong since `acct_init` has always created two. Re-ran the citation/link check across `README.md` + all `docs/*.md`: 305 citations, 0 out of range; 51 links, 0 broken.

## Doc quality (found 2026-08-25, not yet done)
- [x] `fs_create` (`kmain.mx:1587`) is dead code with a stale, misleading comment: it claimed to be "used by `write`'s fast path", but that stopped being true once MortFS grew directories (v2) — `write`'s creation path (`kmain.mx:2302`-`2311`) always resolves the target directory via `fs_parent_of` and calls `fs_create_full` directly, even for root-level files, so `fs_create` (a thin `fs_create_full(name, 255, 0)` wrapper) has no call sites anywhere in the repo (confirmed by grep across every `.mx`/`.py` file: the only hit is its own `fn` line). Found 2026-08-25 while looking for fresh doc gaps — every remaining unchecked backlog item was an explicitly out-of-scope code change (kernel logic or a QEMU-verified test run), so this pass grepped every `fn` definition in `kmain.mx`/`net/*.mx` for call-site counts, same technique that previously found `draw_tab`/`font_data.mxinc`/`net/dns.mx` as dead/unwired code. Done 2026-08-25: rewrote `fs_create`'s comment (`kmain.mx:1586`-`1591`) to state it's dead code and why, citing the real `write` call site instead of the false one. Also fixed two docs that cited `fs_create` as part of the live filesystem core: `docs/architecture.md:201`'s subsystem-summary bullet now names `fs_create_full` (the function `write`/`mkdir`/account setup actually call); `docs/fs-design.md`'s top "Status: implemented" note now cites `fs_create_full` too, with a new parenthetical explaining `fs_create` is the now-dead wrapper this note used to point at. Left the historical Section 1-6 body of `fs-design.md` untouched (it still says `fs_create` in several places, e.g. lines 400/425/518/665) — those sections are the doc's own pre-implementation design prose, already explicitly marked as a kept-as-is historical record predating the v1-to-v2 split, and `fs_create` as originally specced there is what `fs_create_full` grew from; rewriting historical narrative is against this doc's own established convention (see the 2026-08-02 backlog entry above). Comment/doc-only change, no kernel logic touched. Re-ran the citation/link check across `README.md` + all `docs/*.md`: citations in range, links resolve.

## Doc quality (found 2026-08-26, not yet done)
- [x] `docs/networking.md`'s per-layer table cited several `net/*.mx` functions as if they were part of the live receive/verify path, when they're actually dead code — same "audit for drift" pattern as earlier passes, this time turned up a cluster instead of one line. Found 2026-08-26: every remaining unchecked backlog item was an explicitly out-of-scope code change (kernel logic, `build.py`, or something needing a QEMU boot), so this pass re-ran the "grep every `fn` for call-site count" technique that previously found `fs_create`/`draw_tab`/`font_data.mxinc`/`net/dns.mx`, this time across all of `net/*.mx` + `kmain.mx` together (322 `fn` definitions checked). Found a cluster of unused functions the doc's table presented as live: `swap16`/`swap32` and the `hton16`/`ntoh16`/`hton32`/`ntoh32` wrappers around them (`net/endian.mx:11`-`22` — the stack actually uses `be16_load`/`be32_load`/etc. instead); `arp_build_request` (`net/arp.mx:25` — only `arp_build_reply` is ever called, since the kernel never initiates its own ARP request); `mac_eq`/`eth_dst` (`net/eth.mx:23`/`58`); `rtl_send_hello` (`net/rtl8139.mx:196`, a debug broadcast-test-frame helper); and, in every transport-layer `*_verify` function (`ip_verify` `net/ip.mx:50`, `icmp_verify` `net/icmp.mx:17`, `udp_verify` `net/udp.mx:48`, `tcp_verify` `net/tcp.mx:76`) plus `icmp_build_echo_request`/`tcp_window`/`tcp_payload`/`udp_src_port`. Traced *why* the four `*_verify` functions are unused by reading the actual receive dispatch in full — `net_handle_frame` (`net/netcfg.mx:35`-`85`) and the DHCP/TCP handling in `net_dhcp`/`net_httpd` (`net/netapp.mx:82`-`90`, `:203`-`212`) — and confirmed (via `awk` scan of both function bodies) that neither calls any checksum/verify function anywhere: the kernel builds correct checksums on every packet it sends but validates none of what it receives. That's a genuine, previously-undocumented runtime fact, not just dead-code trivia, so it got its own new "Unverified receive path" section rather than a table-cell footnote. Fixed the table rows for Byte order/NIC driver/Ethernet/ARP/IPv4/ICMP/UDP/TCP to name only the functions actually called and flag the rest as unused with citations. Comment/doc-only change, no kernel logic touched; verified every new `file:line` citation against current source line-by-line (not just range-checked) since several were multi-function ranges.

## Doc quality (found 2026-08-27, not yet done)
- [x] `docs/architecture.md`'s "Execution model" section listed three interrupt sources (keyboard, timer, syscall) as driving all post-boot behavior, but a fourth exists and behaves very differently: the 32 CPU exception vectors. Found 2026-08-27 while looking for fresh doc gaps — every remaining unchecked backlog item was an explicitly out-of-scope code change, so this pass read `idt.s`'s exception path (`ISR_STUB` macro, `idt.s:180`-`184`; `isr_common`, `idt.s:219`-`226`) and `on_exception` (`kmain.mx:2629`-`2635`) in full. Done 2026-08-27: added a fourth bullet documenting that unlike the other three sources, an exception's stub never returns (`cli` then an infinite `hlt` loop — no `iret`, no end-of-interrupt) so a fault is unrecoverable and only a reboot clears it, and that `on_exception` prints only the raw vector number — through the same `put_cell_at` (`kmain.mx:505`-`517`) funnel as all other console output, so visible in both VGA text and graphics mode — not a per-vector fault name, demonstrated by the `crash` shell command's `ud2` (vector 6, `#UD`). While grounding the `crash` citation, found `docs/shell.md`'s entire command table had drifted: every `kmain.mx` line number in the Source column from `help` onward, plus several inline citations further down (`exec_file`, `open_power_menu`, `open_lock`, `open_sleep`, `poweroff`, `try_exec_path`, `env_set`'s cap-check, `fs_guard`, `expand_vars`, `run_command`/`run_command_impl`), were each off by exactly 4 lines from current source — traced to an untracked 4-line insertion somewhere between `fs_init` (`kmain.mx:1296`, still correctly cited everywhere) and `fs_create_full` (`kmain.mx:1533`, already correct in `docs/fs-design.md`), meaning different docs drifted out of sync with source at different times rather than from one clean repo-wide shift. Re-verified and corrected every citation in `docs/shell.md` line-by-line against current source (61 spot-checks scripted against the actual file content, not just range-checked) rather than assuming a uniform offset applied everywhere. Re-ran the citation/link check across `README.md` + all `docs/*.md`: 378 citations, 0 out of range; 48 links, 0 broken.
- [x] The `docs/shell.md` citation-drift fix (2026-08-27) covered only that one file; this pass covered the two files it flagged as likely also affected. Done 2026-08-28: verified every `kmain.mx:` citation in `docs/architecture.md` (44 citations) and `docs/accounts.md` (49 citations) individually against current source — not a blanket offset, since the prior pass's own finding held (`docs/fs-design.md`'s `fs_create_full` citation was already current while others weren't). Confirmed drift is real but *localized*: most citations from `on_syscall` (kmain.mx:1864) onward through the account/shell-dispatch region (kmain.mx:2200s-3600s) were stale by exactly +4 lines and got fixed (28 citations in `architecture.md`, 24 in `accounts.md`) — but a contiguous block around `fs_create_full`/`fs_set_meta` (`kmain.mx:1533`-`1593`, cited in `accounts.md`'s permission-bits section) was already correct and needed no change, confirming those exact items from the note above (`fs_create_full` at `kmain.mx:1540`-`1544`) were false alarms. Also fixed one unrelated pre-existing off-by-one, found while verifying: `fs_uid`/`fs_mode`'s citation in `accounts.md` said `kmain.mx:1272`-`1273`, real is `1271`-`1272`. Method: for every citation, read the actual source at the cited line(s) and confirm it matches what the doc says is there — line-count-in-range checks alone would have missed all of this, same lesson as the `shell.md` pass. Re-ran the citation/link check across `README.md` + all `docs/*.md`: 0 citations out of range, 0 broken links.
- [x] The `docs/shell.md`/`docs/architecture.md`/`docs/accounts.md` citation-drift audits (2026-08-27/28) did not cover `README.md`, `docs/memory-map.md`, `docs/fs-design.md`, `docs/programs.md`, or `docs/networking.md` — a quick automated pass (compare each `` `fn_name` `` citation's line number against that function's real `^fn fn_name(` line in `kmain.mx`) turned up candidates in all five, e.g. `README.md`'s `on_key`/`open_launcher`/`acct_init`/`can_write` citations and `docs/fs-design.md`'s `fs_create`/`fs_ensure_layout`/`fs_populate_bin`/`fs_read_file`/`fs_remove`/`ata_write`. Many of these will be false positives the same way `docs/accounts.md`'s `fs_create_full`/`fs_set_meta` citations were (the naive check compares against a function's own `fn` line, but plenty of citations correctly point at a body line or call site instead) — each one needs the same direct read-the-actual-source verification used in the two 2026-08-28 passes above, not the naive script alone. `README.md` is the highest-value next target (it's what a new reader sees first). Found 2026-08-28. Done 2026-08-29 (README.md only, per the item's own recommendation to start there — `docs/memory-map.md`, `docs/fs-design.md`, `docs/programs.md`, and `docs/networking.md` are still unaudited by this method and split out as a follow-up below): read every citation's cited line directly against current source rather than trusting the naive `fn`-line diff. Confirmed real drift, same +4 pattern found in the 2026-08-27/28 passes over a different line range: `on_key`'s F1-F4/F5 dispatch block (`kmain.mx:3503`-`3508` -> `3507`-`3512`), `open_launcher` (`kmain.mx:3362` -> `3366`, both of its two citations), `acct_init` (`kmain.mx:2900` -> `2904`), `login_default`/`cwd_init` (`kmain.mx:2906`/`2912` -> `2910`/`2916`), `can_write` (`kmain.mx:2974` -> `2978`), the boot-banner `print_string` call (`kmain.mx:3599` -> `3603`), the `run_script(1)` call site in the ISO-path paragraph (`kmain.mx:3608` -> `3612` — the other `run_script` citation, `kmain.mx:884`, points at the function's own `fn` line and was already correct), and the `usb_boot_init()` call site (`kmain.mx:3616` -> `3620`). Confirmed several citations were already correct and needed no change: `draw_prompt` (`kmain.mx:709`-`727`), `put_cell_at` (`kmain.mx:505`-`517`), every `net/hardware.mx`/`net/hci_usb.mx`/`net/audio.mx`/`net/settings.mx` citation in the PCI/USB/audio bullet, and every `build.py:` citation in the Windows-only ISO note. Re-ran the repo's link-resolution check across `README.md` + all `docs/*.md`: 0 broken links. Doc-only change, no kernel/build.py logic touched.

## Code follow-ups (found 2026-08-24, all need a local QEMU boot test)
- [ ] `fs_create` (`kmain.mx:1587`) is confirmed dead code (see the doc-quality item above) — a human could `git rm` it, i.e. delete the function, since nothing calls it and MortFS v2's real creation path is `fs_create_full`. Low risk (removing an uncalled function changes no behavior) but still needs a local build to confirm nothing else in the translation unit references it in a way a text grep missed, and a QEMU boot to confirm `write`/`mkdir`/login still work exactly as before. Found 2026-08-25.

- [ ] `rmdir` (`kmain.mx:2235`-`2257`) removes a directory with no permission check whatsoever — it verifies the path resolves, that it's a directory, and that it's empty, then frees the slot. `rm` on a *file* does check (`can_write`, `kmain.mx:2334`-`2338`), and creating an entry inside a directory checks the parent (`fs_create_full`, `kmain.mx:1540`-`1544`), so this looks like an oversight rather than a decision: as uid 1 you cannot create a file in a root-owned `0755` directory, but you can `rmdir` that directory once it's empty. A human could add the `can_write(fs_uid(r), fs_mode(r))` guard to match `rm`'s. Found 2026-08-24 while writing `docs/accounts.md`.
- [ ] The `r` and `x` mode bits are stored, printed by `ls` (`kmain.mx:2201`-`2204`), settable with `chmod`, and never read by anything: `cat` (`kmain.mx:2258`-`2277`) and `exec_file` (`kmain.mx:1892`-`1920`) both act on any entry they can resolve. Only the write bits are live, via `can_write` (`kmain.mx:2974`-`2982`). A code decision for a human — either enforce them (a `can_read`/`can_exec` pair alongside `can_write`, which needs a boot test since `run_script`, `fs_populate_bin` and the `$PATH` fallthrough `try_exec_path` `kmain.mx:2056` all read/exec files during boot as varying uids) or leave them decorative. Documented as-is in `docs/accounts.md` either way. Found 2026-08-24.
- [ ] `passwd` (`kmain.mx:2457`-`2470`) updates `g_acct_hash` in RAM only, so a changed password lasts until reboot — the printed message says so (`password updated (this session)`), and `g_acct_*`'s own declaration comment already calls the table in-memory "persistence comes later" (`kmain.mx:26`). MortFS v2 has directories and an `/etc` seeded at boot (`fs_ensure_layout`, `kmain.mx:1613`-`1621`), so an `/etc/passwd`-style file that `acct_init` reads and `passwd` writes is now actually possible. A real feature for a human, not a doc fix. Found 2026-08-24 while writing `docs/accounts.md`.

## Doc quality (found 2026-08-29, not yet done)
- [x] The README.md citation-drift audit (2026-08-29, see the "did not cover README.md..." item above) only covered `README.md`; `docs/memory-map.md`, `docs/fs-design.md`, `docs/programs.md`, and `docs/networking.md` are still unaudited by the direct read-the-actual-source method (a naive `fn`-line diff flagged candidates in all of them, e.g. `docs/fs-design.md`'s `fs_create`/`fs_ensure_layout`/`fs_populate_bin`/`fs_read_file`/`fs_remove`/`ata_write`, but as with `docs/accounts.md`'s false positives, each candidate needs to be checked against the actual cited line, not assumed stale). Same technique as the four prior passes: for each `` `file:line` `` citation, read that exact line and confirm it matches what the doc claims is there. Done 2026-08-30: audited every `kmain.mx:` citation in the four named docs by reading the exact cited line(s) against current source, not just range-checking. Found and fixed real drift in three of them (`docs/networking.md`'s only `kmain.mx:` citation — the `net`/`httpd` dispatch block — needed the same fix; its `net/*.mx:` citations were already verified by the 2026-08-26 pass and were not re-touched). Root cause, now pinned exactly: the 2026-08-25 pass that marked `fs_create` dead code (see the "Code follow-ups" item below) expanded its one-line comment into a 5-line one (`kmain.mx:1586`-`1590`), net +4 lines — every citation to code *below* that point (roughly `kmain.mx:1587` onward in the old numbering) drifted +4 in every doc not re-audited since; every citation *above* it (`fs_init`, `fs_create_full`, `heap_init`, the VGA/framebuffer helpers, the `MortFS v2` metadata accessors, etc.) was already exactly correct and needed no change — confirming again that a blanket offset is not safe to assume and each citation needs its own check (e.g. `docs/memory-map.md`'s own `exec_enter`/`exec_file` range citation on the "program load base" row was already correct while its "Notes" section's separate `exec_enter` citation two paragraphs down was not, in the same file). Fixed: `docs/memory-map.md` — `SECBUF`/`FILEBUF` usage-site line lists, the whole syscall-mailbox row group (number/arg0/return-value/entry-address/input-buffer addresses and every read/write site), `exec_enter`'s Notes-section citation, and the 120-char input cap line. `docs/fs-design.md` — `fs_read_file`, `fs_remove`, `fs_create`, `fs_ensure_layout`, `fs_populate_bin`, and both quotes of the `bad filesystem` message line. `docs/programs.md` — the `0x00A00000` load-address comment, `exec_file`/`exec_enter`'s cited ranges and the `exec` dispatch line (aligned to the same dispatch-condition-line convention `docs/shell.md` already uses), the mailbox field-table comment, `on_syscall()`'s range, and all four per-syscall body ranges. `docs/networking.md` — the `net`/`httpd` dispatch range. Re-ran the citation-range check (377 citations, 0 out of range) and the link check (48 links, 0 broken) across `README.md` + all of `docs/*.md`. Doc-only change, no kernel/build.py logic touched.

## Doc quality (found 2026-08-31, not yet done)
- [x] `docs/hardware.md`, `docs/settings.md`, and `docs/testing.md` were the only three docs never covered by the 2026-08-27 through 2026-08-30 citation-drift audit passes (those covered `shell.md`, `architecture.md`, `accounts.md`, `README.md`, `memory-map.md`, `fs-design.md`, `programs.md`, `networking.md`). Found 2026-08-31 while looking for fresh doc gaps — every remaining unchecked backlog item was still an explicitly out-of-scope code/build.py/QEMU-test change. Done 2026-08-31: read every `` `file:line` `` citation in all three docs directly against the cited source line (not just range-checking) — `net/hardware.mx`, `net/hci_usb.mx`, `net/audio.mx`, `net/rtl8139.mx`, and every cited `net/settings.mx` line for `docs/hardware.md`/`docs/settings.md`; `test.py`, `test_gfx.py`, and `kmain.mx` for `docs/testing.md`. `docs/settings.md` (56 citations checked) had zero drift — every one matched exactly, including several multi-line function ranges. Found and fixed two real issues in the other two: (1) `docs/hardware.md`'s USB enumeration section named a queue-head *variable* `g_usb_qh` that does not exist anywhere in the source (confirmed by grep across every `.mx` file) — the real identifiers at that citation (`net/hci_usb.mx:28`-`30`) are the functions `usb_qh()` and `usb_td()`; fixed the doc to name the real functions. (2) `docs/testing.md`'s `test_gfx.py` section said `draw_topbar()` is "called from `switch_app()` at `kmain.mx:2841`" — line 2841 is a blank line; `switch_app()` itself starts at `kmain.mx:2843` and its `draw_topbar();` call is at `kmain.mx:2845` — fixed the citation to `2845`, the actual call site. Re-ran the citation-range check (0 out of range) and the link-resolution check (0 broken) across `README.md` + all of `docs/*.md`. Doc-only change, no kernel/build.py logic touched.

## Doc quality (found 2026-09-01, not yet done)
- [x] `docs/shell.md`'s `reboot`/`restart` and `shutdown`/`poweroff` rows named the functions that implement them (`reboot`, `kmain.mx:1024`; `poweroff`, `kmain.mx:3245`) but never said *how* either one actually reaches the hardware — no other doc covers it either. Found 2026-09-01: every remaining unchecked backlog item was still an explicitly out-of-scope code/build.py/QEMU-test change (confirmed by re-reading BACKLOG.md in full), and a fresh audit of `boot.s`/`idt.s`/`linker.ld`/`test_fs.py`/`test_exec.py` citations against `docs/architecture.md`/`docs/memory-map.md`/`docs/testing.md` (the boot-assembly and non-kmain.mx test-harness surface not explicitly named in any prior citation-drift pass) turned up zero drift — all citations there were already exact. Read `reboot()` (`kmain.mx:1024`-`1029`) and `poweroff()` (`kmain.mx:3245`-`3263`) in full instead. Done 2026-09-01: expanded both rows in `docs/shell.md` with the real mechanism, cited: `reboot` pulses the 8042 keyboard controller's reset line (`outb(0x64, 0xFE)`, `kmain.mx:1025`) then falls into a `hlt` loop (`kmain.mx:1026`-`1028`) in case the pulse doesn't land immediately; `poweroff` prints its message, then writes three emulator-specific ACPI shutdown values in sequence — `outw(0x604, 0x2000)`, `outw(0xB004, 0x2000)`, `outw(0x4004, 0x3400)` (`kmain.mx:3256`-`3258`, labeled QEMU/Bochs/VirtualBox in source's own comment, in that port order) — before falling into a `cli`+`hlt` loop (`kmain.mx:3259`-`3262`) for bare metal, where none of those ports do anything. Comment/doc-only change, no kernel logic touched. Re-checked every citation in the edited rows against current source line-by-line.

## Doc quality (found 2026-09-02, not yet done)
- [x] `docs/settings.md`'s Personalization section said the accent color "also colors the selected sidebar row" as its one example of the setting reaching outside the Settings window, but undersold how global `g_accent` actually is: every app's window title bar is painted by the same `window_title()` helper (`kmain.mx:476`-`479`), called by Terminal (`kmain.mx:493`/`2830`), Files (`kmain.mx:2696`), Vex (`kmain.mx:2803`), and Settings itself (`net/settings.mx:442`, plus the dynamic `"settings - connecting..."` title at `net/settings.mx:539`) — none of that was mentioned anywhere in the docs (grepped `window_title` across `README.md`/`docs/*.md`: zero hits before this pass). Found 2026-09-02: every other unchecked backlog item was still an explicitly out-of-scope code/build.py/QEMU-test change (re-read BACKLOG.md, then re-verified a sample of already-audited citations across `docs/shell.md`/`docs/testing.md`/`docs/programs.md` — `run_command`/`expand_vars`/`fs_guard`/`run_command_impl`'s range, `draw_tab`/`draw_topbar`/`switch_app`'s call site, `pstart.s`/`prog.ld`/`build_program`/`_program_sources` — all still exact, no new drift), so this pass looked for genuinely undocumented behavior instead by tracing `g_accent`'s full call-site list (`grep -n g_accent kmain.mx net/*.mx`). Done 2026-09-02: added a paragraph after the existing sidebar-row sentence in `docs/settings.md`'s Personalization section explaining `g_accent` recolors every app's title bar on its next draw, with all five call sites cited. Verified every new `file:line` citation against current source and re-ran the citation-range check (0 out of range) and link-resolution check (0 broken) across `README.md` + all of `docs/*.md`. Doc-only change, no kernel logic touched.
