# Backlog

Real, finishable improvements — the daily agent picks ONE and ships it end-to-end.
Rules: check items off when done, add follow-ups you discover, never do two at once,
smallest reviewable change wins.

**Safety — read this first.** MORT OS is a from-scratch kernel whose behavior can
only be verified by **booting it in QEMU**, and the daily cloud agent has **no QEMU
and cannot build or boot the kernel**. So the agent works on **documentation and
non-behavioral text only** — `README.md`, files under `docs/`, and source code
*comments*. It must **not** change kernel logic (the behavior of any `*.mx` file),
`build.py` behavior, assembly (`*.s`), or linker scripts — those require a local
QEMU boot test that a human runs. Every factual claim in a doc must be grounded in
the actual source (cite `file:line`); if the only useful work would touch kernel
behavior, add a backlog item describing it and stop.

## Documentation
- [x] `docs/architecture.md` — the boot chain, execution model, and major subsystems, cited to source. Done 2026-07-29: covers both boot paths (`qemu-system-i386 -kernel` vs. the Limine ISO), the `_start` → `kernel_setup` → `mort_kmain` handoff (`boot.s`, `idt.s`), the `kmain()` init order (`kmain.mx:3589-3625`), the three interrupt sources that drive all post-boot behavior (keyboard/timer/syscall), the four-app desktop dispatch (`g_app`, `switch_app`, `kmain.mx:2839-2850`), and the `exec`/syscall mailbox ABI — linking out to the four existing detail docs rather than repeating them. Corrected two inaccuracies in this backlog item's own description while writing it: the kernel never transitions to long mode (stays 32-bit protected mode throughout — verified in `boot.s`/`idt.s`), and the filesystem is MortFS, not "VexFS" (Vex is the unrelated browser-app tribute, `g_app == 2`). Also confirmed, contrary to the item's wording, there is no scheduler anywhere in the source — documented that absence explicitly instead of describing a scheduler that doesn't exist. Linked from README's "How it fits together" section.
- [x] `docs/memory-map.md` — a single table of every fixed address the kernel uses, each row citing the source line that defines it. Done 2026-07-26: kernel load (1 MiB), VGA buffer (`0xB8000`), disk buffers (SECBUF/TABCACHE/FILEBUF), syscall mailbox (`0x009F0000`-`0x009F0179`), program load base (`0x00A00000`, not `0x01000000` — that address is actually the heap base per `kmain.mx:1043`, so this item's own description had the wrong address; corrected in the doc), heap base/top. Framebuffer address is dynamic (read from multiboot info), noted as such rather than listed as fixed.
- [x] `docs/shell.md` — a reference table of the built-in shell commands (read them out of `kmain.mx`), one line of behavior each. Done 2026-07-25: every command in `run_command_impl` (`kmain.mx:2100`-`2602`), cited by line, plus `$VAR` expansion and command history.
- [x] `docs/networking.md` — the mortnet layers (RTL8139 → ARP → IPv4 → UDP → DHCP → DNS → TCP → HTTP) and which `net/*.mx` file implements each. Done 2026-07-28: a layer table citing every builder/parser function by `file:line`, plus a "what `net`/`httpd` actually do" section tracing the real runtime path through `net/netapp.mx`'s `net_dhcp`/`net_httpd`. Linked from README's Networking section. Also noted, grounded in a grep across the repo: `net/dns.mx` is fully implemented and host-testable but is never called from the kernel (no shell command or `netapp.mx` path invokes it), and none of `test.py`/`test_fs.py`/`test_exec.py`/`test_gfx.py` exercise `net`/`httpd` at all.
- [x] README: a "build and boot it yourself" quickstart — the real `build.py` subcommands and QEMU invocation, with the expected first lines of serial output. Verify command names against the actual `build.py`. Done 2026-07-31: the existing quickstart's commands already matched `build.py`'s `COMMANDS` dict, so the gap was the "expected output" half — and there is no serial console in this kernel (confirmed: no serial I/O anywhere in `kmain.mx`/`boot.s`/`idt.s`), so that part of the item's own premise was wrong; documented the real VGA/framebuffer console output instead. Added two grounded transcripts: the bare `-kernel` path (`build.py run`) shows just the boot banner (`kmain.mx:3599`) and prompt, since `run_script(1)` (`kmain.mx:884`) finds no multiboot module without an ISO and no-ops; the `run-iso` path additionally runs the baked-in `STARTUP_TXT` (`build.py:88-90`) via `run_script(1)` at `kmain.mx:3608`, but boots straight to the home launcher overlay (`open_launcher`, `kmain.mx:3362`), so the transcript is only visible after pressing `Esc` — noted that, since a reader who doesn't press Esc would otherwise never see it. Traced every intervening init call (`fs_init`, `fs_ensure_layout`, `fs_populate_bin`, `acct_init`, `login_default`, `cwd_init`) for stray output before writing the transcript; none print anything.

## Doc quality
- [ ] Audit `README.md` and `docs/` for claims that have drifted from the current source (feature list vs. what is actually implemented) and correct them, citing the source for each fix. Partial progress 2026-08-06: spot-checked every `file:line` citation in `README.md`, `docs/architecture.md`, `docs/shell.md`, and most of `docs/memory-map.md` against the current source (`kmain.mx`, `boot.s`, `idt.s`, `linker.ld`) — all matched, nothing stale found there. The real drift found was a missing feature, not a stale citation: `net/hardware.mx` (PCI bus scan), `net/hci_usb.mx` (UHCI USB driver), and `net/audio.mx` (AC'97 audio) are fully implemented and wired into the Settings app's "Hardware & Devices" page (`settings_hardware`, `net/settings.mx:280`) but were entirely absent from README's "What it does" list — fixed with a new grounded bullet. Also fixed `kmain.mx:49`'s `g_app` comment, which listed only 3 of the 4 apps (`0 terminal, 1 files, 2 vex` — missing `3 settings`), found while cross-checking the app-switch dispatch (`kmain.mx:3510-3518`) against it. Still unaudited: `docs/networking.md`'s `net/*.mx` citations beyond the NIC driver row, `docs/fs-design.md`'s historical sections 1-5, and `mkfs.py`/`build.py` internals. Further progress 2026-08-07: audited every `file:line` citation in `docs/networking.md`'s layer table and its "What `net`/`httpd` actually do" section (`net/endian.mx`, `net/checksum.mx`, `net/rtl8139.mx`, `net/eth.mx`, `net/arp.mx`, `net/ip.mx`, `net/icmp.mx`, `net/udp.mx`, `net/dhcp.mx`, `net/dns.mx`, `net/tcp.mx`, `net/http.mx`, `net/netcfg.mx`, `net/netapp.mx`, plus `kmain.mx:2131-2137`'s `net`/`httpd` dispatch). All matched except the NIC driver row: `rtl_init`, `rtl_poll_rx`, and `rtl_transmit` were each cited one line past their actual `fn` line (`net/rtl8139.mx:64`/`129`/`175` vs. the real `63`/`128`/`174`) — fixed all three. Still unaudited: `docs/fs-design.md`'s historical sections 1-5, and `mkfs.py`/`build.py` internals.
- [x] `docs/fs-design.md` opens with "Nothing here is implemented yet; this is the spec to implement from" (line 3), but MortFS is fully implemented and shipping (`kmain.mx` has `fs_init`/`fs_read_file`/etc., `docs/shell.md` documents `ls`/`cat`/`write`/`rm`/`run` as working commands). Done 2026-07-27: replaced the header with a "Status: implemented" note citing the real functions (`fs_init` `kmain.mx:1296`, `fs_create` `kmain.mx:1587`, `fs_read_file` `kmain.mx:1662`, `fs_remove` `kmain.mx:1732`, `ata_init`/`ata_read`/`ata_write` `kmain.mx:1175`-`1241`, `g_run_depth` guard `kmain.mx:40`/`895`) and flagging that Sections 0-5 are historical (drafting-time paths like `kernel/kmain.mx`/`kernel/mkfs.py`/`mort/typechecker.py` were not rewritten, only the header), Section 6 is the executed plan not a to-do, Section 7's non-goals are current limitations. Body sections 1-5 left untouched per the item's own scope.
- [x] `docs/fs-design.md` sections 1 and 5.1 still say `kernel/kmain.mx` and `kernel/mkfs.py` — the real files are `kmain.mx` and `mkfs.py` at the repo root. Done 2026-08-02: fixed the copy-pasteable commands in Section 5 (`### 5.1 mkfs.py`, `### 5.2 build.py` headers and their code blocks) to the real flat-layout paths, since those are invocations a reader could actually try to run. Left the historical narrative prose alone (e.g. "drafted as `kernel/kmain.mx` below" in the intro) per the doc's own header explaining Sections 0-5 are a kept-as-is historical record — only the runnable bits were broken, so only those were fixed. While grounding the Section 2 quote of the kernel's `bad filesystem` message against the actual source, found it doesn't match verbatim (doc said "run:", source says "host:" — `kmain.mx:1751`); corrected the quote and discovered the source string itself still hard-codes the stale `kernel/mkfs.py` path, which is a genuine source bug (see new item below), not a doc bug — left the quote showing that stale path since it's what the kernel actually prints.
- [ ] `kmain.mx:1751`'s `bad filesystem` message hard-codes a stale path: `"bad filesystem (host: python kernel/mkfs.py disk.img)"` — the real invocation is `python mkfs.py build/disk.img` (`mkfs.py` lives at the repo root, not under `kernel/`, and `build.py`'s own hints at `build.py:345`/`372` point at `build/disk.img`). A one-line string fix for a human with a local QEMU boot test to confirm the new message still fits the print budget; found while grounding `docs/fs-design.md`'s quote of this message against the actual source.
- [x] `power`, `lock`, and `sleep` shell commands silently no-op in VGA text mode — `open_power_menu` (`kmain.mx:3145`), `open_lock` (`kmain.mx:3199`), and `open_sleep` (`kmain.mx:3229`) all `return` immediately `if !g_gfx`. Worth a doc note (or, as a code change for a human with a local QEMU boot test, a `no display` message instead of silent no-op). Done 2026-08-04: this item's own premise was only a third right — read all three functions and only `open_power_menu` actually silently no-ops; `open_lock` and `open_sleep` both print an explicit message (`"lock needs the graphical desktop"` / `"sleep needs the graphical desktop"`, `kmain.mx:3200`-`3203` and `kmain.mx:3230`-`3233`) before returning. Documented the real per-command behavior in `docs/shell.md`'s `power`/`lock`/`sleep` rows, which previously said nothing about text-mode at all. Left the code as-is (no message for `power` in text mode) — a human with a local QEMU boot test could add one, matching the pattern `open_lock`/`open_sleep` already use, per this item's own suggestion.
- [x] `net/rtl8139.mx`'s header comment (line 1) says the file lives at `glue/rtl8139.mx`; the real path is `net/rtl8139.mx`. Same comment also says "M1 does transmit only — receive arrives with ARP in M2" (line 10-11), but receive is already implemented in this same file (`rtl_poll_rx`, `net/rtl8139.mx:129`) — a stale milestone note from an earlier development stage. Small comment fix, found while writing `docs/networking.md`. Done 2026-08-01: fixed the path (`net/rtl8139.mx`), dropped the now-false "lives in `glue/` rather than `net/`" rationale, and replaced the transmit-only milestone note with a line naming both `rtl_transmit` and `rtl_poll_rx` (the latter polled from `net/netapp.mx:79`/`195`, confirmed by grep) as implemented.
- [ ] `net/dns.mx` is a fully implemented, host-tested DNS A-record resolver that no shell command or kernel code path ever calls (confirmed by grepping for `dns_` outside that file) — found while writing `docs/networking.md`. Either wire it up (e.g. an `httpd` variant that resolves a configured hostname, or a `nslookup`-style shell command) as a code change for a human with a local QEMU boot test, or leave as-is and just keep the doc note that it's currently dead code.
- [x] README's "What it does" list only names three apps (Terminal, Files, Vex browser) switched with F1/F2/F3, but the source has four: `on_key` (`kmain.mx:3503-3508`) maps F1-F4 to `switch_app(0..3)` where app `3` is a Settings app (`settings_on_key`/`settings_draw`, dispatched at `kmain.mx:2849`), and F5 opens a home launcher (`open_launcher`, `kmain.mx:3362`) not mentioned in the README at all. Found while writing `docs/architecture.md`; feeds the "audit README for drifted claims" item above. Done 2026-07-30: fixed both the intro paragraph and the "What it does" bullet to name all four apps (Terminal, Files, Vex, Settings — the latter backed by `net/settings.mx`, confirmed a real personalization/clock/network/hardware control center, not a stub) plus the F5 launcher, citing `kmain.mx:3503-3508` and `kmain.mx:3362`.
- [x] `net/settings.mx` implements the Settings app (`settings_draw`/`settings_on_key`) but lives under `net/` alongside the actual networking stack — it's pulled in only because `build.py`'s `build()` (`build.py:110-119`) glues every `net/*.mx` file into the same translation unit as `kmain.mx`. Not a bug (build works fine), just a confusing location for a file with nothing to do with networking; worth a one-line comment at the top of `net/settings.mx` noting why it's there, or a human could move it to a top-level file as a build.py-touching change. Found while verifying the app-list fix above. Done 2026-08-03: added a comment block at the top of `net/settings.mx` explaining the file's location, citing the actual glue loop (`build.py:113-121`, confirmed by reading the current file — line numbers had drifted slightly from this item's original `build.py:110-119`). Comment-only change, no behavior touched.
- [x] `draw_tab()` (`kmain.mx:453-462`) — a helper for drawing a highlighted app-switcher tab in the top bar — is defined but never called anywhere in the source (confirmed by grep); `draw_topbar()` (`kmain.mx:467-473`) only draws static "F5 home"/"F12 power" hint text, no per-app tabs. Either dead code a human can remove, or an unfinished feature; found while verifying the app-list fix above. Done 2026-08-05: `draw_topbar()`'s own header comment was itself stale — it claimed to draw "app switcher tabs (F1/F2/F3)" when the function body never does. Fixed both comments (source-comment-only change, verified line count unchanged so no other doc's `kmain.mx:` citations drifted): `draw_tab()` now notes it's unused, `draw_topbar()` now describes what it actually draws and points at `draw_tab()` as the unused helper. No other doc referenced "app switcher tabs" so nothing else needed correcting. The underlying dead-code-or-unfinished-feature question is still a human call — left as a new backlog item below since it requires a code decision, not a doc one.
- [ ] `draw_tab()` (`kmain.mx:453-462`) is a fully-working unused helper — a code-level decision, not a doc one: either wire it into `draw_topbar()` to actually render F1-F4 tabs (an unfinished feature), or delete it as dead code. Either way needs a local QEMU boot test to confirm the top bar still renders correctly. Left open when correcting `draw_topbar()`'s stale comment (2026-08-05); this item is the follow-up code work the comment fix doesn't cover.
- [ ] There is no serial console anywhere in this kernel (`boot.s`, `idt.s`, `kmain.mx` — grepped, nothing touches COM1/0x3F8 or any UART). All console output is VGA text (`put_index`) or a drawn framebuffer console (`draw_glyph`), both behind `put_cell_at` (`kmain.mx:505-517`). Worth a one-line note wherever a reader might assume a serial log exists (there is currently no such assumption in the docs, but the original wording of the README-quickstart backlog item above assumed one — future doc work should not repeat that assumption). Found while grounding the 2026-07-31 README quickstart addition.
- [ ] The `run-iso` boot path opens straight to the home launcher overlay (`open_launcher`, `kmain.mx:3619`, called unconditionally when `g_gfx`) rather than the terminal, so first-time users booting the ISO see icon tiles, not a shell prompt, until they press `Esc` or `Enter` on Terminal. Not necessarily a bug, but worth confirming is the intended first-boot experience — a human with a local QEMU boot test could decide whether to change it (code change, out of scope for the docs agent) or just leave it as designed. Found while writing the ISO transcript in the README quickstart addition (2026-07-31).
