# Backlog

Real, finishable improvements — the daily agent picks ONE and ships it end-to-end.
Rules: check items off when done, add follow-ups you discover, never do two at once,
smallest reviewable change wins.

**Safety — read this first.** MORT OS is a from-scratch kernel whose behavior can
only be verified by **booting it in QEMU**, and the daily cloud agent has **no QEMU
and cannot build or boot the kernel**. So the agent works on **documentation and
non-behavioral text only** — `README.md`, files under `docs/`, and source code
*comments*. It must **not** change kernel logic (the behavior of any `*.mx` file),
`build.py` behavior, assembly (`*.s`), or linker scripts — those require a local
QEMU boot test that a human runs. Every factual claim in a doc must be grounded in
the actual source (cite `file:line`); if the only useful work would touch kernel
behavior, add a backlog item describing it and stop.

## Documentation
- [x] A `docs/programs.md` reference for MORT OS's user-program format and syscall ABI. Done 2026-08-09: no doc previously gathered the four-syscall ABI (print+newline, print inline, uptime, read-line), the mailbox layout, and the `programs/*.mx` build pipeline (`pstart.s`/`prog.ld`, `build_program()` `build.py:274`-`320`) in one place — it was scattered across a one-paragraph summary in `docs/architecture.md`, an address-only table in `docs/memory-map.md`, and comments duplicated across the three `programs/*.mx` sample files. Wrote a grounded reference: what a program is, the build pipeline cited to `build.py`, a full syscall table cited to `on_syscall()` (`kmain.mx:1860`-`1881`), the calling convention shown via real snippets from `programs/hello.mx`/`ask.mx`, and how `_program_sources()` (`build.py:323`-`328`) auto-discovers any new `.mx` file dropped in `programs/`. Linked from README's "Running programs" section and `docs/architecture.md`'s "Programs and syscalls" section. Verified every `file:line` citation against current source and checked all markdown links resolve (script-checked, not just spot-checked).
- [x] `docs/architecture.md` — the boot chain, execution model, and major subsystems, cited to source. Done 2026-07-29: covers both boot paths (`qemu-system-i386 -kernel` vs. the Limine ISO), the `_start` → `kernel_setup` → `mort_kmain` handoff (`boot.s`, `idt.s`), the `kmain()` init order (`kmain.mx:3589-3625`), the three interrupt sources that drive all post-boot behavior (keyboard/timer/syscall), the four-app desktop dispatch (`g_app`, `switch_app`, `kmain.mx:2839-2850`), and the `exec`/syscall mailbox ABI — linking out to the four existing detail docs rather than repeating them. Corrected two inaccuracies in this backlog item's own description while writing it: the kernel never transitions to long mode (stays 32-bit protected mode throughout — verified in `boot.s`/`idt.s`), and the filesystem is MortFS, not "VexFS" (Vex is the unrelated browser-app tribute, `g_app == 2`). Also confirmed, contrary to the item's wording, there is no scheduler anywhere in the source — documented that absence explicitly instead of describing a scheduler that doesn't exist. Linked from README's "How it fits together" section.
- [x] `docs/memory-map.md` — a single table of every fixed address the kernel uses, each row citing the source line that defines it. Done 2026-07-26: kernel load (1 MiB), VGA buffer (`0xB8000`), disk buffers (SECBUF/TABCACHE/FILEBUF), syscall mailbox (`0x009F0000`-`0x009F0179`), program load base (`0x00A00000`, not `0x01000000` — that address is actually the heap base per `kmain.mx:1043`, so this item's own description had the wrong address; corrected in the doc), heap base/top. Framebuffer address is dynamic (read from multiboot info), noted as such rather than listed as fixed.
- [x] `docs/shell.md` — a reference table of the built-in shell commands (read them out of `kmain.mx`), one line of behavior each. Done 2026-07-25: every command in `run_command_impl` (`kmain.mx:2100`-`2602`), cited by line, plus `$VAR` expansion and command history.
- [x] `docs/networking.md` — the mortnet layers (RTL8139 → ARP → IPv4 → UDP → DHCP → DNS → TCP → HTTP) and which `net/*.mx` file implements each. Done 2026-07-28: a layer table citing every builder/parser function by `file:line`, plus a "what `net`/`httpd` actually do" section tracing the real runtime path through `net/netapp.mx`'s `net_dhcp`/`net_httpd`. Linked from README's Networking section. Also noted, grounded in a grep across the repo: `net/dns.mx` is fully implemented and host-testable but is never called from the kernel (no shell command or `netapp.mx` path invokes it), and none of `test.py`/`test_fs.py`/`test_exec.py`/`test_gfx.py` exercise `net`/`httpd` at all.
- [x] README: a "build and boot it yourself" quickstart — the real `build.py` subcommands and QEMU invocation, with the expected first lines of serial output. Verify command names against the actual `build.py`. Done 2026-07-31: the existing quickstart's commands already matched `build.py`'s `COMMANDS` dict, so the gap was the "expected output" half — and there is no serial console in this kernel (confirmed: no serial I/O anywhere in `kmain.mx`/`boot.s`/`idt.s`), so that part of the item's own premise was wrong; documented the real VGA/framebuffer console output instead. Added two grounded transcripts: the bare `-kernel` path (`build.py run`) shows just the boot banner (`kmain.mx:3599`) and prompt, since `run_script(1)` (`kmain.mx:884`) finds no multiboot module without an ISO and no-ops; the `run-iso` path additionally runs the baked-in `STARTUP_TXT` (`build.py:88-90`) via `run_script(1)` at `kmain.mx:3608`, but boots straight to the home launcher overlay (`open_launcher`, `kmain.mx:3362`), so the transcript is only visible after pressing `Esc` — noted that, since a reader who doesn't press Esc would otherwise never see it. Traced every intervening init call (`fs_init`, `fs_ensure_layout`, `fs_populate_bin`, `acct_init`, `login_default`, `cwd_init`) for stray output before writing the transcript; none print anything.

- [x] A `docs/hardware.md` reference for the PCI/USB/audio subsystem (`net/hardware.mx`, `net/hci_usb.mx`, `net/audio.mx`) — the only subsystem with real implementation depth but no dedicated doc, only a one-paragraph README bullet. Done 2026-08-11: wrote a grounded reference covering `hw_scan_pci`'s class/subclass table (`net/hardware.mx:12`-`34`) and that it only runs on-demand from Settings, not at boot; the PC speaker (`net/hardware.mx:36`-`58`); `ethernet_disconnect` (`net/hardware.mx:60`-`66`); the full UHCI boot-time enumeration sequence in `net/hci_usb.mx` (controller find/reset, root-port probing, the hand-built TD/QH control-transfer pipeline for GET_DESCRIPTOR/SET_ADDRESS/GET_CONFIGURATION, and the Bluetooth-HCI-class heuristic that's the only thing behind `g_hw_bluetooth`) — including the "polling until IRQ support is added" comment (`net/hci_usb.mx:205`) confirming it runs before interrupts are enabled; and AC'97 init/volume/test-tone in `net/audio.mx`, also on-demand not at-boot. Caught and fixed one of my own draft claims before committing: I initially wrote that the Settings volume-cycle control could momentarily display above 100, but tracing `(g_audio_volume + 25) % 125` through `ac97_set_volume`'s own clamp (script-verified) showed the clamp happens before the value is stored, so the display never actually exceeds 100 — corrected. Also documented a small but genuine gotcha: Settings' "PS/2 keyboard" row always prints the literal string `"Ready"` (`net/settings.mx:283`) with no actual presence probe behind it. Linked from README's hardware bullet, `docs/architecture.md`'s kernel-entry section, and `docs/networking.md`'s "unrelated to mortnet" disclaimer. Verified every `file:line` citation and markdown link with the same script-based check used in prior passes (0 out of range, 0 broken).
- [x] A `docs/settings.md` reference for the Settings app (`net/settings.mx`, 623 lines) — it had real surface area (13 sidebar sections, 5 interactive sub-pages, a keyword search overlay) but no dedicated doc, only passing mentions in README/`docs/hardware.md`. Done 2026-08-13: read the file in full and wrote a grounded reference: the 13-section sidebar table (which sections are read-only vs. open a sub-page, each cited to its draw function); the `g_settings_view`/`g_settings_section`/`g_settings_sel`/`g_settings_scroll` navigation state machine (`settings_up`/`settings_down`/`settings_back`/`settings_enter`, `net/settings.mx:452`-`545`); the Personalization, Clock, Accessibility, and Ethernet sub-pages (Hardware controls deliberately deferred to `docs/hardware.md`, cross-linked, since that doc already covers it in depth); and the search overlay's fixed 16-keyword index, substring matcher (`settings_matches`, `:78`-`96`), and result-to-section mapping. Caught one genuine drift while grounding the doc: the `g_settings_view` field comment (`net/settings.mx:11`) only listed 3 of its 6 actual values (missing accessibility/hardware/network, added after the comment was written) — fixed the comment in place, kept it on one line so no line numbers after it shifted (verified: file still 623 lines, and existing `net/settings.mx:` citations in `docs/hardware.md`/`README.md`/this file, all below line 11, still match). Linked from README's app-list bullet and `docs/architecture.md`'s window-manager section. Verified every `file:line` citation against current source and checked every markdown link/anchor across `README.md` and `docs/*.md` resolves with a script (0 out of range, 0 broken).
- [x] A `docs/testing.md` reference for the four test scripts (`test.py`, `test_fs.py`, `test_exec.py`, `test_gfx.py`) — README only ever listed the invocation commands, never what each one actually checks. Done 2026-08-12: wrote a grounded reference covering what each script boots/seeds and asserts (read all four scripts in full to write this, not just their docstrings), plus a table documenting the shared harness API `test.py` exports (`boot`/`type_line`/`screen_text`/`wait_for`/`shutdown`) that `test_fs.py`/`test_exec.py` build on. While tracing `test_gfx.py`'s per-app top-bar tab assertions (`test_gfx.py:105`-`140`, checking pixel colors at fixed coordinates for "tab active"/"tab inactive") against current source to cite them accurately, found they test for a feature that doesn't exist in `kmain.mx` as currently written: `draw_topbar()` (`kmain.mx:467`-`473`) draws no per-app tabs (only the OS label, F5/F12 hints, and the clock), and `draw_tab()` (`kmain.mx:453`-`462`) — the helper that would draw one — is confirmed dead code by grep, consistent with this backlog's own existing item about it. Documented the mismatch in `docs/testing.md` rather than editing `test_gfx.py` itself (Python test assertions are outside this agent's docs-only scope, and I have no QEMU to confirm whether the assertion currently passes or fails) — added a follow-up item below for a human to check. Linked from README's testing section. Verified every `file:line` citation and markdown link with the same script-based check used in prior passes (0 out of range, 0 broken).

- [x] `docs/architecture.md` never described how the Mort source actually becomes the multiboot ELF that its own "Boot chain" section boots — `build.py`'s `build()` was undocumented outside a one-paragraph mention in README's ASCII diagram. Done 2026-08-16: added a "Build pipeline" section between "Boot chain" and "Kernel entry" covering all four real steps, each grounded and cited: (1) `net/*.mx` is concatenated with `kmain.mx` into one translation unit before compiling (`build.py:109-122`) — the same glue loop `net/settings.mx`'s header comment already points at; (2) cross-compilation to `x86-freestanding-none` via Zig (`_zig()`, `build.py:96-103`), including a genuinely undocumented "why" worth surfacing: the `-mno-sse`/`-mno-sse2`/`-mno-mmx` flags exist because SSE is disabled at runtime (`CR4.OSFXSR=0`) and an `-O2` vectorized `memset` would otherwise fault (`#UD`) on real hardware (`build.py:127-132`); (3) `runtime.c`'s freestanding `memset`/`memcpy`/`memmove`, compiled and linked in exactly like `kmain.c`/`boot.s`/`idt.s` (`build.py:139-146`, `runtime.c:1-4`) — this file wasn't mentioned in any doc before today; (4) the final link into `build/kernel.elf` and what `build.py check` validates on it (`build.py:148-153`, `check()` `build.py:158-190`). Cross-linked to `docs/programs.md`'s existing build-pipeline section for the parallel (but differently-linked) `programs/*.mx` path. Verified every `file:line` citation against current source; no new links added (referenced files already had entries elsewhere in the doc).

## Doc quality
- [x] Audit `README.md` and `docs/` for claims that have drifted from the current source (feature list vs. what is actually implemented) and correct them, citing the source for each fix. Partial progress 2026-08-06: spot-checked every `file:line` citation in `README.md`, `docs/architecture.md`, `docs/shell.md`, and most of `docs/memory-map.md` against the current source (`kmain.mx`, `boot.s`, `idt.s`, `linker.ld`) — all matched, nothing stale found there. The real drift found was a missing feature, not a stale citation: `net/hardware.mx` (PCI bus scan), `net/hci_usb.mx` (UHCI USB driver), and `net/audio.mx` (AC'97 audio) are fully implemented and wired into the Settings app's "Hardware & Devices" page (`settings_hardware`, `net/settings.mx:280`) but were entirely absent from README's "What it does" list — fixed with a new grounded bullet. Also fixed `kmain.mx:49`'s `g_app` comment, which listed only 3 of the 4 apps (`0 terminal, 1 files, 2 vex` — missing `3 settings`), found while cross-checking the app-switch dispatch (`kmain.mx:3510-3518`) against it. Still unaudited: `docs/networking.md`'s `net/*.mx` citations beyond the NIC driver row, `docs/fs-design.md`'s historical sections 1-5, and `mkfs.py`/`build.py` internals. Further progress 2026-08-07: audited every `file:line` citation in `docs/networking.md`'s layer table and its "What `net`/`httpd` actually do" section (`net/endian.mx`, `net/checksum.mx`, `net/rtl8139.mx`, `net/eth.mx`, `net/arp.mx`, `net/ip.mx`, `net/icmp.mx`, `net/udp.mx`, `net/dhcp.mx`, `net/dns.mx`, `net/tcp.mx`, `net/http.mx`, `net/netcfg.mx`, `net/netapp.mx`, plus `kmain.mx:2131-2137`'s `net`/`httpd` dispatch). All matched except the NIC driver row: `rtl_init`, `rtl_poll_rx`, and `rtl_transmit` were each cited one line past their actual `fn` line (`net/rtl8139.mx:64`/`129`/`175` vs. the real `63`/`128`/`174`) — fixed all three. Still unaudited: `docs/fs-design.md`'s historical sections 1-5, and `mkfs.py`/`build.py` internals. Further progress 2026-08-08: audited `docs/fs-design.md` against `mkfs.py` and the current `kmain.mx` FS code — found the biggest drift yet. MortFS is now v2, not v1: `mkfs.py` writes `version = 2` (`mkfs.py:31`) and `fs_init` (`kmain.mx:1296`-`1332`) auto-upgrades any v1 disk in place on first boot. v2 adds directories, Unix-style permissions (`mode`), and ownership (`uid`) in the 12 bytes of each file-table entry that Section 2.3 called "reserved" — `type`/`parent`/`uid`/`mode`/`mtime`, accessors at `kmain.mx:1269`-`1273`. None of this was in `docs/fs-design.md` at all, and its Section 7 non-goals still claimed "No directories" as current. `docs/shell.md` (audited 2026-08-06, still accurate) already documents the resulting `cd`/`mkdir`/`rmdir`/`chmod`/`chown` commands, so only `fs-design.md` was stale. Fixed: added a grounded "MortFS has since grown a v2 format" note up top citing every relevant function, corrected the Section 2.3 reserved-bytes row, and corrected the Section 7 non-goals bullet (left the stale text visible with a strikethrough-style note rather than silently deleting a direct quote of the original v1 list, per this doc's own established convention for historical sections). Also fixed `mkfs.py`'s docstring and `--help` text, which still said `python kernel/mkfs.py`/`python kernel/build.py`/`kernel/docs/fs-design.md` (comment/docstring only — no argparse behavior changed, verified with `--help`). Still unaudited: `docs/fs-design.md` Sections 1 and 3-6 line-by-line (only the format-version and directory claims were checked against source; the ATA driver section (1) and buffer/shell-spec sections (3-6) were not re-verified this pass), and `build.py` internals. Further progress 2026-08-09: audited `docs/fs-design.md` Section 1 (ATA PIO driver spec) and Section 3 (buffer addresses) line-by-line against `kmain.mx`'s `ata_init`/`ata_wait_bsy`/`ata_wait_drq`/`ata_read`/`ata_write` and the `SECBUF`/`TABCACHE`/`FILEBUF` constants — every port number, status-bit mask, and address matched exactly, including the `0xFC` PIC mask claim (verified against `idt.s:131`). Also mechanically checked every `file:line` citation across `README.md` and all of `docs/*.md` with a script that resolves each citation against the cited file's actual line count (0 out of range) and a second script that resolves every markdown link (0 broken) — a cheap check worth re-running on future passes since it catches the citation-drifted-after-an-edit-elsewhere failure mode that spot-checking misses. Spot-verified `build.py`'s `net/*.mx` glue-loop citation in `net/settings.mx` (`build.py:113`-`121`, still accurate) and the `STARTUP_TXT`/`ensure_disk`/`disk()` citations behind README's ISO quickstart (still accurate). Sections 4-6 remain deliberately unaudited per-claim: the doc's own header already discloses they're historical/pre-implementation prose superseded by `docs/shell.md`, so auditing them further would just restate that disclosure. `build.py` internals are now covered by this pass and the new `docs/programs.md` item above (its build-pipeline section audited `build_program()`/`_program_sources()`/`prog()` in full). Net effect: no remaining unaudited surface found to be materially stale — treating this item as complete unless a future pass turns up new drift after further source changes.
- [x] `docs/fs-design.md` opens with "Nothing here is implemented yet; this is the spec to implement from" (line 3), but MortFS is fully implemented and shipping (`kmain.mx` has `fs_init`/`fs_read_file`/etc., `docs/shell.md` documents `ls`/`cat`/`write`/`rm`/`run` as working commands). Done 2026-07-27: replaced the header with a "Status: implemented" note citing the real functions (`fs_init` `kmain.mx:1296`, `fs_create` `kmain.mx:1587`, `fs_read_file` `kmain.mx:1662`, `fs_remove` `kmain.mx:1732`, `ata_init`/`ata_read`/`ata_write` `kmain.mx:1175`-`1241`, `g_run_depth` guard `kmain.mx:40`/`895`) and flagging that Sections 0-5 are historical (drafting-time paths like `kernel/kmain.mx`/`kernel/mkfs.py`/`mort/typechecker.py` were not rewritten, only the header), Section 6 is the executed plan not a to-do, Section 7's non-goals are current limitations. Body sections 1-5 left untouched per the item's own scope.
- [x] `docs/fs-design.md` sections 1 and 5.1 still say `kernel/kmain.mx` and `kernel/mkfs.py` — the real files are `kmain.mx` and `mkfs.py` at the repo root. Done 2026-08-02: fixed the copy-pasteable commands in Section 5 (`### 5.1 mkfs.py`, `### 5.2 build.py` headers and their code blocks) to the real flat-layout paths, since those are invocations a reader could actually try to run. Left the historical narrative prose alone (e.g. "drafted as `kernel/kmain.mx` below" in the intro) per the doc's own header explaining Sections 0-5 are a kept-as-is historical record — only the runnable bits were broken, so only those were fixed. While grounding the Section 2 quote of the kernel's `bad filesystem` message against the actual source, found it doesn't match verbatim (doc said "run:", source says "host:" — `kmain.mx:1751`); corrected the quote and discovered the source string itself still hard-codes the stale `kernel/mkfs.py` path, which is a genuine source bug (see new item below), not a doc bug — left the quote showing that stale path since it's what the kernel actually prints.
- [ ] `kmain.mx:1751`'s `bad filesystem` message hard-codes a stale path: `"bad filesystem (host: python kernel/mkfs.py disk.img)"` — the real invocation is `python mkfs.py build/disk.img` (`mkfs.py` lives at the repo root, not under `kernel/`, and `build.py`'s own hints at `build.py:345`/`372` point at `build/disk.img`). A one-line string fix for a human with a local QEMU boot test to confirm the new message still fits the print budget; found while grounding `docs/fs-design.md`'s quote of this message against the actual source.
- [x] `power`, `lock`, and `sleep` shell commands silently no-op in VGA text mode — `open_power_menu` (`kmain.mx:3145`), `open_lock` (`kmain.mx:3199`), and `open_sleep` (`kmain.mx:3229`) all `return` immediately `if !g_gfx`. Worth a doc note (or, as a code change for a human with a local QEMU boot test, a `no display` message instead of silent no-op). Done 2026-08-04: this item's own premise was only a third right — read all three functions and only `open_power_menu` actually silently no-ops; `open_lock` and `open_sleep` both print an explicit message (`"lock needs the graphical desktop"` / `"sleep needs the graphical desktop"`, `kmain.mx:3200`-`3203` and `kmain.mx:3230`-`3233`) before returning. Documented the real per-command behavior in `docs/shell.md`'s `power`/`lock`/`sleep` rows, which previously said nothing about text-mode at all. Left the code as-is (no message for `power` in text mode) — a human with a local QEMU boot test could add one, matching the pattern `open_lock`/`open_sleep` already use, per this item's own suggestion.
- [x] `net/rtl8139.mx`'s header comment (line 1) says the file lives at `glue/rtl8139.mx`; the real path is `net/rtl8139.mx`. Same comment also says "M1 does transmit only — receive arrives with ARP in M2" (line 10-11), but receive is already implemented in this same file (`rtl_poll_rx`, `net/rtl8139.mx:129`) — a stale milestone note from an earlier development stage. Small comment fix, found while writing `docs/networking.md`. Done 2026-08-01: fixed the path (`net/rtl8139.mx`), dropped the now-false "lives in `glue/` rather than `net/`" rationale, and replaced the transmit-only milestone note with a line naming both `rtl_transmit` and `rtl_poll_rx` (the latter polled from `net/netapp.mx:79`/`195`, confirmed by grep) as implemented.
- [ ] `net/dns.mx` is a fully implemented, host-tested DNS A-record resolver that no shell command or kernel code path ever calls (confirmed by grepping for `dns_` outside that file) — found while writing `docs/networking.md`. Either wire it up (e.g. an `httpd` variant that resolves a configured hostname, or a `nslookup`-style shell command) as a code change for a human with a local QEMU boot test, or leave as-is and just keep the doc note that it's currently dead code.
- [x] README's "What it does" list only names three apps (Terminal, Files, Vex browser) switched with F1/F2/F3, but the source has four: `on_key` (`kmain.mx:3503-3508`) maps F1-F4 to `switch_app(0..3)` where app `3` is a Settings app (`settings_on_key`/`settings_draw`, dispatched at `kmain.mx:2849`), and F5 opens a home launcher (`open_launcher`, `kmain.mx:3362`) not mentioned in the README at all. Found while writing `docs/architecture.md`; feeds the "audit README for drifted claims" item above. Done 2026-07-30: fixed both the intro paragraph and the "What it does" bullet to name all four apps (Terminal, Files, Vex, Settings — the latter backed by `net/settings.mx`, confirmed a real personalization/clock/network/hardware control center, not a stub) plus the F5 launcher, citing `kmain.mx:3503-3508` and `kmain.mx:3362`.
- [x] `net/settings.mx` implements the Settings app (`settings_draw`/`settings_on_key`) but lives under `net/` alongside the actual networking stack — it's pulled in only because `build.py`'s `build()` (`build.py:110-119`) glues every `net/*.mx` file into the same translation unit as `kmain.mx`. Not a bug (build works fine), just a confusing location for a file with nothing to do with networking; worth a one-line comment at the top of `net/settings.mx` noting why it's there, or a human could move it to a top-level file as a build.py-touching change. Found while verifying the app-list fix above. Done 2026-08-03: added a comment block at the top of `net/settings.mx` explaining the file's location, citing the actual glue loop (`build.py:113-121`, confirmed by reading the current file — line numbers had drifted slightly from this item's original `build.py:110-119`). Comment-only change, no behavior touched.
- [x] `draw_tab()` (`kmain.mx:453-462`) — a helper for drawing a highlighted app-switcher tab in the top bar — is defined but never called anywhere in the source (confirmed by grep); `draw_topbar()` (`kmain.mx:467-473`) only draws static "F5 home"/"F12 power" hint text, no per-app tabs. Either dead code a human can remove, or an unfinished feature; found while verifying the app-list fix above. Done 2026-08-05: `draw_topbar()`'s own header comment was itself stale — it claimed to draw "app switcher tabs (F1/F2/F3)" when the function body never does. Fixed both comments (source-comment-only change, verified line count unchanged so no other doc's `kmain.mx:` citations drifted): `draw_tab()` now notes it's unused, `draw_topbar()` now describes what it actually draws and points at `draw_tab()` as the unused helper. No other doc referenced "app switcher tabs" so nothing else needed correcting. The underlying dead-code-or-unfinished-feature question is still a human call — left as a new backlog item below since it requires a code decision, not a doc one.
- [ ] `draw_tab()` (`kmain.mx:453-462`) is a fully-working unused helper — a code-level decision, not a doc one: either wire it into `draw_topbar()` to actually render F1-F4 tabs (an unfinished feature), or delete it as dead code. Either way needs a local QEMU boot test to confirm the top bar still renders correctly. Left open when correcting `draw_topbar()`'s stale comment (2026-08-05); this item is the follow-up code work the comment fix doesn't cover.
- [x] There is no serial console anywhere in this kernel (`boot.s`, `idt.s`, `kmain.mx` — grepped, nothing touches COM1/0x3F8 or any UART). All console output is VGA text (`put_index`) or a drawn framebuffer console (`draw_glyph`), both behind `put_cell_at` (`kmain.mx:505-517`). Worth a one-line note wherever a reader might assume a serial log exists (there is currently no such assumption in the docs, but the original wording of the README-quickstart backlog item above assumed one — future doc work should not repeat that assumption). Found while grounding the 2026-07-31 README quickstart addition. Done 2026-08-10: added the note right after README's quickstart transcript (the one place a reader watching QEMU output might reach for a serial log instead), citing the same grep result and `put_cell_at` (`kmain.mx:505-517`, verified exact) as the funnel point for both VGA and framebuffer output. Re-confirmed the grep: no `serial`/`0x3F8`/`COM1`/`UART` hit anywhere in `boot.s`, `idt.s`, or `kmain.mx`.
- [ ] The `run-iso` boot path opens straight to the home launcher overlay (`open_launcher`, `kmain.mx:3619`, called unconditionally when `g_gfx`) rather than the terminal, so first-time users booting the ISO see icon tiles, not a shell prompt, until they press `Esc` or `Enter` on Terminal. Not necessarily a bug, but worth confirming is the intended first-boot experience — a human with a local QEMU boot test could decide whether to change it (code change, out of scope for the docs agent) or just leave it as designed. Found while writing the ISO transcript in the README quickstart addition (2026-07-31).
- [ ] `test_gfx.py`'s per-app top-bar tab assertions (`test_gfx.py:105`-`140`) check for pixel colors that no current source path produces — see `docs/testing.md`'s `test_gfx.py` section for the full trace (`draw_topbar()` draws no tabs; `draw_tab()`, the helper that would, is unused dead code per the existing `draw_tab()` backlog item above). A human with a local QEMU + ISO toolchain should actually run `test_gfx.py` to confirm whether these specific checks currently pass or fail. If they fail, this is the same underlying code decision as the `draw_tab()` item (wire it up or remove it) plus a test fix; if they somehow pass, this doc's reasoning has a gap worth finding. Found 2026-08-12 while writing `docs/testing.md`.
- [x] `python build.py iso`/`run-iso` (and `test_gfx.py`, which calls `build.iso()` directly) only work on Windows — undocumented anywhere. Done 2026-08-14: read `build.py` in full looking for fresh doc gaps (BACKLOG's own doc items were all already checked off) and found `iso()` (`build.py:219`-`271`) fetches `limine.exe`/`xorriso.exe` — Windows PE binaries, `LIMINE_ZIP`/`XORRISO_ZIP` at `build.py:65`-`66` — and executes them directly via `subprocess.run` (`build.py:261`-`269`), with `_cygpath()` (`build.py:212`-`216`) converting paths to the Cygwin form `xorriso.exe` expects; no Wine invocation or OS branch anywhere in the script. Added a grounded note to README's "A real bootable ISO" section and to `docs/testing.md`'s harness-requirements paragraph (linking back to the README note), so a Linux/macOS reader hits this before an opaque exec-format failure instead of after. `build`/`check`/`run`/`window`/`disk`/`prog`/`test.py`/`test_fs.py`/`test_exec.py` are unaffected — confirmed none of them call `iso()` or touch `TOOLS`/`LIMINE_ZIP`/`XORRISO_ZIP`.
- [ ] `build.py iso()` fails on non-Windows hosts with a raw `subprocess`/OS-level exec error (attempting to run a fetched `.exe` directly, `build.py:261`/`269`) rather than a clear message. A human with the ability to test on Linux/macOS could add an early `sys.platform` check in `iso()` that exits with a clear "Windows only (or install/invoke via Wine)" message instead — a `build.py`-behavior change, out of scope for the docs-only agent, and small enough to verify without a QEMU boot (just needs running `python build.py iso` on a non-Windows host to confirm the current failure mode first). Found 2026-08-14 while grounding the Windows-only ISO note above.
- [x] `font_data.mxinc` (repo root, 258 lines) declares a `g_font: [u8; 4096]` bitmap font but is never referenced by `build.py` or any `*.mx`/`*.s` file. Done 2026-08-15: all prior doc-item leads in this backlog were either already checked off or explicitly out-of-scope code changes, so this pass re-read the repo root for undocumented surface and found this file by grepping for `font_data` across the whole tree (zero hits outside the file itself). Diffed it byte-for-byte against the `g_font` array actually compiled into the kernel (`kmain.mx:74`-`331`, used by `draw_glyph`, `kmain.mx:420`) — identical. Added a header comment to `font_data.mxinc` itself (comment-only, no behavior change) explaining it's an orphaned duplicate, and a new "Orphaned files" section in `docs/architecture.md` with the same finding, cited and cross-linked. Left the file in place rather than deleting it — deleting an unreferenced file is safe (nothing includes it) but is a repo-structure change outside this pass's README/docs/comments scope; noted below as a follow-up a human can do in one command.
- [ ] `git rm font_data.mxinc` — confirmed dead/orphaned (see item above, `docs/architecture.md`'s "Orphaned files" section, and the header comment now on the file itself). Deleting it changes no kernel behavior since nothing includes it, so this is safe for a human to do without a QEMU boot test; left undone by the docs-only agent since removing a tracked file is a repo-structure change, not documentation.
